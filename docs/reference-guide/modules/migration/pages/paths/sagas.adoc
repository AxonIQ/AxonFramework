= Saga Migration
:navtitle: Sagas
:reftext: Saga migration

Axon Framework 5 has no `Saga` construct. Replace it with existing core framework building blocks.
The familiar `@Saga` stereotype and related APIs are not part of the Axon Framework 5 core; a legacy layer allows
migrating running sagas, and the same applies to `DeadlineManager` and related APIs.

For *new* processes, Axon Framework 5 provides several ways to implement them with the existing framework building blocks.
The xref:{page-component-version}@saga-guide:ROOT:index.adoc[Sagas and Process Managers] guide describes them in detail.

For sagas that are *already running* (considering business processes running for months), rewriting is not an option.
That case is served by the `axon-legacy` module, described in <<legacy-module>>.

[IMPORTANT]
====
For saga migration you need to consider two scenarios:

*Writing new processes:* Pick an approach from the
xref:{page-component-version}@saga-guide:ROOT:index.adoc[Sagas and Process Managers] guide and write it today.

*Migrating running sagas:* Use the Axon Framework 4 compatible APIs of the `axon-legacy` module to keep existing saga classes running until all legacy saga instances are drained naturally.
The legacy module should be treated as a way to finish existing instances, not to keep sagas in Axon Framework 5.
====

[#legacy-module]
== Running existing sagas on Axon Framework 5

The `axon-legacy` module allows migrating to Axon Framework 5 while saga instances started on Axon Framework 4 are still running.
Business processes covered by sagas can span weeks or months.
`axon-legacy` lets running saga instances continue while new instances start on an Axon Framework 5 native implementation of the business process.

The `axon-legacy` module brings forward the following Axon Framework 4 saga infrastructure, adapted to the Axon Framework 5 APIs:

* `@SagaEventHandler`, `@StartSaga`, `@EndSaga`, `SagaLifecycle` and the association-value mechanism
* `SagaStore` and its concrete implementations, to support existing saga stores

The `org.axonframework.spring.stereotype.Saga` annotation itself keeps its Axon Framework 4 package but ships in the
Spring extension (`axon-spring`), not in `axon-legacy`. Spring Boot autoconfiguration for legacy sagas activates only
when both `axon-legacy` and `axon-spring-boot-starter` are on the classpath; see <<spring-boot-sagas>>.

The legacy module supports existing saga classes as-is. Add `axon-legacy` as a dependency to your project.

[NOTE]
====
The saga side of `axon-legacy` (link:https://github.com/AxonFramework/AxonFramework/issues/3728[GitHub Issue #3728]),
including Spring Boot autoconfiguration, targets 5.4.0. Deadlines
(link:https://github.com/AxonFramework/AxonFramework/issues/3065[GitHub Issue #3065] and
link:https://github.com/AxonFramework/AxonFramework/issues/5006[GitHub Issue #5006]) are not yet ported: `DeadlineManager`
and `@DeadlineHandler` remain outstanding. Until they land, plan on the code-level migration below and on the
drainage strategy in <<strategy>> for the deadline part of a saga.
====

The `axon-legacy` module is a migration aid, not a supported way to write new processes. `@StartSaga` is deprecated.
Use the legacy module to let existing instances receive their events and run to completion while new work goes to the
Axon Framework 5 implementation written alongside it. A ported `@StartSaga` handler can still create instances until
you remove or disable that annotation as part of the drainage strategy.

[#spring-boot-sagas]
== Configuring legacy sagas with Spring Boot

Add `axon-legacy` next to `axon-spring-boot-starter` as a dependency. Autoconfiguration for legacy sagas activates
once `axon-legacy` is on the classpath; no further setup is needed for a `@Saga`-annotated, component-scanned class to
start receiving events.

=== Processor assignment

Each `@Saga` class gets its own pooled streaming event processor, named `<SimpleName>Processor` (for example,
`OrderSagaProcessor` for `OrderSaga`). The processor starts at the head of the stream (the latest token), so a saga
ignores events that occurred before the processor first starts, matching the Axon Framework 4
`TrackingEventProcessor` default for sagas.

The head token survives anything that configures the processor without setting an initial token of its own:
`axon.eventhandling.processors.<name>.*` entries, and an `EventProcessorDefinition` that only tunes settings. Tuning
a saga's processor never changes which events the saga sees.

Making a saga process the stream from the start requires setting an initial token explicitly, through a
`SagaProcessorDefinition`. It runs after the head-token default.

[source,java]
----
include::example$migration/paths/sagas/SagaProcessorConfiguration.java[tag=replay-into-saga,indent=0]
----

[NOTE]
====
Axon Framework 4 reached the same outcome by a different route. There, customizing a saga's processor dropped the
saga's head-token default, and the processor fell back to the generic tracking processor default of
`createReplayToken(createHeadToken())`: it read from the start of the stream, but flagged every event up to the head
as a replay, and a saga manager reports `supportsReset() == false`, so those events were never delivered to it.
Either way, a saga saw only new events.

The Axon Framework 5 generic default resolves to `createReplayToken(firstToken)`, whose replay window is empty, so
that fallback no longer protects a saga: without the head-token default an existing stream reaches the saga as
ordinary events and re-executes its side effects. Keeping the head token in place is what preserves the Axon
Framework 4 behavior.
====

Every `axon.eventhandling.processors.<name>.*` property (`mode`, `source`, `token-claim-interval`, `thread-count`,
`batch-size`, and `token-store`) applies to a saga's processor as it applies to any other processor. A pooled saga
processor needs a `TokenStore` bean, as does any other pooled processor.

`initial-segment-count` is the exception: a saga's processor runs one segment, as in Axon Framework 4, whose saga
processor defaults derive from single-threaded processing. A saga manager sequences as `BROADCAST`, so every segment
reads every event and then keeps only the sagas whose identifier it owns; extra segments multiply reads of the whole
event stream and token store rows without splitting the stream. The property cannot raise it, because the properties
always carry a segment count and cannot express "leave it alone", so honoring them would fan a saga out over sixteen
segments as soon as an unrelated property such as `batch-size` was set. Raise it deliberately with a
`SagaProcessorDefinition`, see <<configuring-a-sagas-processor>>.

[WARNING]
====
`mode=subscribing` switches the saga to a subscribing processor, which has no segments and no token store. Every
application instance then falls back to the root segment and starts its own instance of the same saga. This limit is
inherited from Axon Framework 4, where a subscribing processor behaved the same way. Keep sagas on a pooled
streaming processor in a multi-instance deployment.
====

Processor names derived from a saga's simple name are mixed-case (for example, `SimpleSagaProcessor`). Relaxed
property binding lowercases a dotted key and silently fails to match, so target the processor with bracket notation
instead:

[source,properties]
----
axon.eventhandling.processors[SimpleSagaProcessor].mode=subscribing
----

`@Namespace` on the saga type replaces `@ProcessingGroup`: it overrides the derived processor name. Two saga classes
carrying the same `@Namespace` value (or deriving the same default name) share one processor, reproducing the Axon
Framework 4 behavior of co-locating sagas in one processing group.

[WARNING]
====
Keep the Axon Framework 4 processor name, or in-flight sagas lose events.

`@ProcessingGroup` does not exist in Axon Framework 5, so a saga carrying it no longer compiles. Removing the
annotation renames the processor to `<SimpleName>Processor`. A `TokenStore` is keyed on the processor name, so the
Axon Framework 4 token is then orphaned, the renamed processor starts at the head of the stream, and every event
between the old token position and start-up is never delivered. Sagas already in flight, the ones this module exists
to carry across, silently miss them.

Replace `@ProcessingGroup("orders")` with `@Namespace("orders")`, keeping the value, so the existing token is found:

[source,java]
----
include::example$migration/paths/sagas/OrderSaga.java[tag=namespace-preserves-token,indent=0]
----

This applies to any saga whose Axon Framework 4 processor name was not `<SimpleName>Processor`, including one named
through `assignProcessingGroup` or a hand-registered `SagaConfiguration`.
====

Sagas are assembled separately from ordinary Spring event handlers, so that no part of saga support has to be
threaded through the handler discovery every Axon Framework 5 application uses. Processor settings and
`EventProcessorDefinition` beans still apply to a saga's processor as they apply to any other, and an
`EventProcessorDefinition` selector can assign a saga to its named processor.

[#configuring-a-sagas-processor]
=== Configuring a saga's processor

Declare a `SagaProcessorDefinition` bean. It reaches the same `PooledStreamingEventProcessorConfiguration` an
ordinary processor exposes, and runs after everything else, so it can set the initial token, the segment count, an
executor, or anything else:

[source,java]
----
include::example$migration/paths/sagas/SagaProcessorConfiguration.java[tag=saga-segments,indent=0]
----

`forSaga(OrderSaga.class)` follows the saga wherever its processor ended up, including onto a `@Namespace`-renamed
one, so it never has to restate how the name was derived.

The saga type selects the processor; it does not narrow the customization to that saga. A processor is the unit of
configuration, so when two sagas share one, `forSaga(OrderSaga.class)` configures the processor carrying both. Use
`forProcessor("orders")` there, which says so in the code:

[source,java]
----
include::example$migration/paths/sagas/SharedSagaProcessorConfiguration.java[tag=shared-saga-processor,indent=0]
----

Selecting a shared processor with `forSaga` still works and is logged, naming the other sagas it also configures.

`customized(...)` covers the settings both modes share: the error handler, the unit of work factory, and
extensions. It never mentions a mode.

A pooled streaming and a subscribing processor differ beyond that, so a customization touching mode-specific
settings says which mode it was written against. `whenPooledStreaming(...)` takes a
`PooledStreamingEventProcessorConfiguration` with its initial token and segment count; `whenSubscribing(...)` takes a
`SubscribingEventProcessorConfiguration` with its own `eventSource` and `ignoredMessageHandler`.

[source,java]
----
include::example$migration/paths/sagas/SubscribingSagaProcessorConfiguration.java[tag=subscribing-saga-processor,indent=0]
----

The `when` prefix is literal: naming the mode does not select it. A saga's mode comes from
`axon.eventhandling.processors.<name>.mode` or a matching `EventProcessorDefinition`, and a definition written for
the other mode is skipped, and logged, so a customization never quietly does nothing. Letting an adjustment switch
the processor's kind would be a far larger change than the adjustment itself: it decides whether there is a token
store, whether there are segments, and whether a second application instance starts a duplicate saga.

`PooledStreamingEventProcessorModule.Customization` beans are the one mechanism that does *not* apply to a saga's
processor, and `SagaProcessorDefinition` is their saga-scoped replacement. That channel is how cross-cutting Axon
Framework 5 infrastructure attaches itself to every processor in an application. Dead-lettering is enabled that way,
for one, and Axon Framework 4 never dead-lettered a saga: it never wrapped a saga manager in its dead-lettering
invoker. A saga would otherwise silently acquire behavior the original never had, so a saga's processor opts out of
the channel entirely rather than guessing which customization was meant for it. Nothing declares a
`SagaProcessorDefinition` except a saga's own configuration, so the same cannot happen through it.

An `EventProcessorDefinition` naming the processor applies as well, for an application that configures sagas the way
it configures every other processor.

[NOTE]
====
Prefer `SagaProcessorDefinition` for a saga. An `EventProcessorDefinition` has to answer two questions a saga has
already settled: which handlers it assigns, which for a saga is nobody, and the processor's mode. A definition built
with `EventProcessorDefinition.pooledStreaming(...)` therefore overrules `mode=subscribing` on that processor, even
when it only sets an unrelated value such as a batch size.

A `SagaProcessorDefinition` written with `whenPooledStreaming(...)` is skipped, with a warning, on a saga running in
subscribing mode, and the other way round. `customized(...)` applies either way.
====

One consequence: a saga and an ordinary event handler resolving to the same processor name are rejected, and startup
fails with a `DuplicateModuleRegistrationException`. Axon Framework 4 co-located them on one processor when they
shared a processing group. To combine them on purpose, drop `@Saga` from the class and register a `Module` bean
directly, combining a declarative saga component with autodetected handlers.

=== Saga store selection

A saga's store is resolved in this order:

. A user-defined `SagaStore` bean.
. A `JpaSagaStore` (bean name `sagaStore`), when an `EntityManagerFactory` bean is present. `SagaEntry` and
`AssociationValueEntry` are registered with the persistence unit automatically.
. A `JdbcSagaStore` (bean name `sagaStoreNoSchema` or `sagaStoreWithSchema`), when a `DataSource` bean is present. A
user-declared `SagaSqlSchema` bean selects the schema-aware variant.
. An `InMemorySagaStore` (bean name `sagaStore`), as the final fallback.

`@Saga(sagaStore = "beanName")` overrides the store for one saga type, resolving the named bean instead of the one in
the chain above.

=== Suppressing autoconfiguration for one saga

Discovery registers a `SpringSagaConfigurer` bean named `<sagaBeanName>$$Registrar` for every `@Saga` bean. Declaring
a bean under that name yourself keeps discovery from configuring that saga.

[IMPORTANT]
====
Discovery ported an Axon Framework 4 quirk literally: if a `<sagaBeanName>$$Registrar` bean already exists when
discovery reaches it, discovery aborts for *all* sagas processed after that point in bean-definition order, not just
the one being suppressed. Prefer opting a saga out of `@Saga` discovery entirely (drop the annotation and register it
through a `Module` bean, see <<behavior-changes>>) over relying on this hook for anything but the last saga bean
discovered.
====

[#behavior-changes]
== Behavior changes from Axon Framework 4

* *Pooled streaming replaces the tracking processor.* A saga's processor is pooled streaming, not a
`TrackingEventProcessor`. A `TokenStore` bean is required, as for any pooled processor. The head-token default
preserves the Axon Framework 4 outcome that a saga only ever sees new events, and it survives tuning; replaying into
a saga requires setting an initial token explicitly, see <<spring-boot-sagas>>.
* *No resource injection.* `ResourceInjector` and `SpringResourceInjector` are not ported. A saga's collaborators
arrive as handler method parameters instead, resolved the same way as for any other event handler, including Spring
beans.
* *No `@ProcessingGroup`.* `@Namespace` on the saga type is the replacement for renaming or co-locating a saga's
processor. It co-locates sagas with each other only: a saga and an ordinary event handler resolving to one processor
name fail at startup, where Axon Framework 4 put both on one processor. See <<spring-boot-sagas>> for the workaround.
* *Dead-letter queues are not supported for sagas*, as in Axon Framework 4: the saga invoker was never wrapped in
dead-lettering there either. Enabling a dead letter queue for a saga's processor by name has no effect, because a
saga's processor does not take part in the `PooledStreamingEventProcessorModule.Customization` channel that turns
dead-lettering on, see <<spring-boot-sagas>>.
* *Deadlines are not ported yet* (see the note in <<legacy-module>>).
* *The in-memory saga store is an explicit, overridable bean*, not an internal default. An application that wants a
default store alongside named per-saga stores declares the default store itself.
* *A `@Saga` class must be public with an accessible no-argument constructor*, matching the Axon Framework 4
requirement that the default saga factory does not call `setAccessible`.
* *Component-scan saga classes; do not declare them with a `@Bean` method.* `@Saga` is `@Scope("prototype")`, and
skipping prototype beans is what keeps a saga out of ordinary event handler discovery. A `@Bean` method ignores the
scope declared on the class, so a saga declared that way is registered twice: as its saga manager, and again as a
plain event handling component on the package-derived processor, which then routes every event to one shared saga
instance outside any association or lifecycle handling. Axon Framework 4 behaves the same way, so this is pinned
rather than rejected.

== Mapping Axon Framework 4 saga concepts to Axon Framework 5

An Axon Framework 4 saga provides opinionated technical infrastructure to implement long-running business processes:

* State is *state-sourced*: the saga instance is serialized into a `SagaStore` as an opaque blob
* Correlation requires *association values*, maintained with `SagaLifecycle.associateWith(...)`.
* Life cycle is framework-managed, through `@StartSaga` and `@EndSaga`.

Axon Framework 5 provides several ways to implement long-running processes, described in
xref:{page-component-version}@saga-guide:ROOT:index.adoc[Sagas and Process Managers].
The following table maps Axon Framework 4 saga concepts to their Axon Framework 5 equivalents.

[cols="1,2",options="header"]
|===
| Axon Framework 4 | Axon Framework 5

| `@Saga`
| A `@Workflow` (see <<workflows,Workflows>>), or an ordinary component with `@EventHandler` methods.

| `@SagaEventHandler(associationProperty = "...")`
| `awaitEvent(...)` in combination with `associate(...)` for a <<workflows,Workflow>>, or a plain `@EventHandler` resolving correlation by the entity identifier, a property, or your own `EntityIdResolver`.

| `@StartSaga`
| `@Workflow(startOnEvent = "...")` for a <<workflows,Workflow>>. For a plain `@EventHandler` approach, the first event that concerns a process creates its state.

| `@EndSaga`
| The end of the workflow definition for a <<workflows,Workflow>>. For a plain `@EventHandler`, depending on the approach: delete the row, append a completion event, or do nothing when state is derived.

| `SagaLifecycle.associateWith(...)`
| `awaitEvent(...)` in combination with `associate(...)` for a <<workflows,Workflow>>, a tag on the event, or a derived correlation value.

| `SagaLifecycle.end()`
| a `return` statement in the workflow definition for a <<workflows,Workflow>>, or whatever the chosen approach records as "finished".

| `SagaStore`, `SagaRepository`
| Your regular event store for a <<workflows,Workflow>>, a repository of your own, an event-sourced entity, or nothing.

| `DeadlineManager`, `@DeadlineHandler`
| `sleep(...)` for a <<workflows,Workflow>>, or a projection of outstanding work plus a scheduled sweep that sends a command. See
xref:{page-component-version}@saga-guide:ROOT:deadlines.adoc[deadlines].

| `SagaTestFixture`
| The dedicated <<workflows,Workflows>> Test infrastructure, or `AxonTestFixture` publishing events in the given phase and asserting on dispatched commands for a plain `@EventHandler` approach.
|===

[#workflows]
[.axoniq-feature]
== Replacing a saga with a workflow

The recommended replacement for sagas is xref:{page-component-version}@saga-guide:ROOT:workflows.adoc[Workflows]: a process is written as a
single method that reads top to bottom, using the regular event store for event-sourced persistence.

[NOTE]
====
Workflows are an Axoniq Framework feature and require an Axoniq license to run in production.
Workflows are currently available as a preview and can be tried out without a license.
====

Saga concepts map onto Workflow concepts:

* The association value becomes the workflow's identifier and manual `associate(...)` invocations when waiting on events.
* Each `@SagaEventHandler` becomes a step.
* `@EndSaga` becomes the end of the workflow definition method.
* Deadlines are expressed as explicit wait or timeout on an event in the workflow.

Workflows differ from Axon Framework 4 sagas in how they hold state: they source it from dedicated workflow events in
the regular event store instead of a persistent `SagaStore` blob.
A running saga holding a serialized instance in a `SagaStore` cannot be handed to a workflow. It must drain as
described in <<strategy>>, whichever replacement you pick.

[#deadlines]
== Migrating deadlines

Axon Framework 4 scheduled deadlines to defer reactions for sagas.
Axon Framework 5 has no built-in deadline scheduling. Use other capabilities to get the same effect.

In Axon Framework 4, `DeadlineManager` schedules a deadline to trigger in the future; `@DeadlineHandler` handles the
trigger, typically by sending a command.

In Axon Framework 5, use a scheduling library of your choice: wire in the `CommandGateway`, and send the command
through the gateway when the scheduled task triggers.
An idempotent command handler ignores an action scheduled for a process that has already progressed.

See xref:{page-component-version}@saga-guide:ROOT:deadlines.adoc[deadlines] for details on deadlines for long-running processes in Axon
Framework 5.

[#choosing]
== Choosing a replacement

Not every saga should become a workflow.
Simple processes consisting of a limited set of independent reactions can use regular Axon Framework 5 event and
command handlers.
The xref:{page-component-version}@saga-guide:ROOT:choosing-an-approach.adoc[guide] compares the alternatives in detail:

* xref:{page-component-version}@saga-guide:ROOT:state-in-a-repository.adoc[State in a repository] is the closest to how an Axon Framework 4 saga worked.
Start here if you are porting an existing saga.
* xref:{page-component-version}@saga-guide:ROOT:state-from-context-events.adoc[State from context events] avoids persistent state in a repository.
This is only possible if all involved contexts write to one event store and their events carry a tag you can select on.
* xref:{page-component-version}@saga-guide:ROOT:state-from-process-events.adoc[State from process events] suits integration with systems whose
events are not yours, and gives an audit trail of the process.
* xref:{page-component-version}@saga-guide:ROOT:vertical-slices.adoc[Vertical slices] break the process into independent reactions. This fits
Vertical Slice Architecture and Event Modelling.

[#strategy]
== Migrating an application with running sagas

Whether or not you use the <<legacy-module,legacy module>>, migrate running saga instances by *drainage*: let saga
instances created by Axon Framework 4 finish normally, while new process instances start on an Axon Framework 5
implementation.

Without the legacy module, drain old saga instances in an Axon Framework 4 deployment kept alive alongside the Axon
Framework 5 application running the migrated process.
The legacy module allows you to drain the old saga instances alongside the upgraded application in a single Axon Framework 5 application.
Either way (using the legacy-module for drainage or not), new sagas must not be started from the Axon Framework 4 legacy code.

The main steps for migrating running sagas:

. *Stop starting new sagas.* Remove the `@StartSaga` annotation from the Axon Framework 4 saga.
Existing instances keep receiving their events and run to completion; no new ones are created.

+
[source,java]
----
@Saga
public class PaymentSaga {

    // @StartSaga  <1>
    @SagaEventHandler(associationProperty = "bikeId")
    public void on(BikeRequestedEvent event) {
        // ...
    }
}
----
<1> Removing this annotation stops new instances from starting; existing instances can still advance and end.

. *Keep the old saga running.* With the legacy module, add the dependency and let the existing class continue against its existing saga store in the upgraded application.
Without it, keep the Axon Framework 4 deployment alive to drain existing saga instances without creating new ones.
. *Implement the process in Axon Framework 5* using one of the approaches above, and let it handle the same trigger
event.
. *Wait for the old instances to drain.* Monitor the `SagaStore` for active instances and watch the count fall to zero.
How long that takes depends on your longest-running process.
. *Retire the Axon Framework 4 saga* once there are no more active instances in the `SagaStore`: remove the legacy saga code, the `SagaStore` table, its deadline scheduler, and the legacy dependency.

Scheduled deadlines drain the same way: keep the Axon Framework 4 `DeadlineManager`, or the legacy module's port of
it, online as long as saga instances still rely on it for deadlines.

== Testing

A saga kept running on the <<legacy-module,legacy module>> keeps its existing tests.
Write new tests for the process migrated to Axon Framework 5 with `AxonTestFixture`: publish events that drive the
process and assert on the commands it sends.

The mapping below covers the common cases:

[cols="1,2",options="header"]
|===
| `SagaTestFixture` | `AxonTestFixture`

| `givenNoPriorActivity()`
| `given().noPriorActivity()`

| `givenAPublished(event)` / `andThenAPublished(event)`
| `given().events(event, ...)`

| `whenPublishingA(event)`
| `when().event(event)`, or include it in `given().events(...)` and assert with `await`

| `expectDispatchedCommands(command)`
| `then().await(result -> result.commandsSatisfy(...))`

| `expectActiveSagas(n)`
| No equivalent. Assert on the observable outcome, or on your own state if the approach keeps any.

| `whenTimeElapses(duration)`
| No equivalent. Structure the timeout so it can be invoked directly; see xref:{page-component-version}@saga-guide:ROOT:deadlines.adoc[deadlines].
|===


== A working example

`examples/saga-recipes` in the
link:https://github.com/AxonFramework/AxonFramework[Axon Framework repository,role=external,window=_blank] implements the bike-rental payment saga from an Axon Framework 4 sample application, using every approach described here.
