package commands.configuration.builder;

import org.axonframework.modelling.repository.Repository;

public class GiftCardCommandHandler {

    public GiftCardCommandHandler(Repository<?, ?> giftCardRepository) {
    }
}
