\* Every rule of the append protocol, on an engine that speaks the boundary
\* protocol (INFINITY_IS_ORIGIN = FALSE).  This is the control run: the two
\* registry invariants and every rule check must hold together, otherwise a
\* violation in another run says nothing about that run's flag.
\* EXPECT: No error.
SPECIFICATION Spec

CONSTANTS
    Writers = {w1, w2}
    Events <- MCEvents
    Boundaries <- MCBoundaries
    Batches <- MCBatches
    MaxLen = 3
    INFINITY_IS_ORIGIN = FALSE

INVARIANT TypeOK
INVARIANT AppendConformsToDcbModel
INVARIANT UnconditionalAppendNeverRejected
INVARIANT AppendIsLegalIffNoMatchInScanRange
INVARIANT CommitPositionsAndMarkerFollowTheRules
INVARIANT CommitMarkerNeverRegresses
