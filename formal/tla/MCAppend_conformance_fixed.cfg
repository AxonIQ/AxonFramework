\* Conformance with the marker collapse off.  The engine and the reference model
\* are then the same decision procedure, so they cannot disagree.
\* EXPECT: No error.
SPECIFICATION Spec

CONSTANTS
    Writers = {w1, w2}
    Events <- MCEvents
    Boundaries <- MCBoundaries
    Batches <- MCBatches
    MaxLen = 3
    INFINITY_IS_ORIGIN = FALSE

INVARIANT TypeOK
INVARIANT AppendConformsToDcbModel
