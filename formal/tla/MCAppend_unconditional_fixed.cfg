\* The same invariant with the marker collapse off, which is the modelled fix:
\* an INFINITY marker claims no boundary, so the conflict check is skipped
\* rather than run against the whole store.
\* EXPECT: No error.
SPECIFICATION Spec

CONSTANTS
    Writers = {w1, w2}
    Events <- MCEvents
    Boundaries <- MCBoundaries
    Batches <- MCBatches
    MaxLen = 3
    INFINITY_IS_ORIGIN = FALSE

INVARIANT TypeOK
INVARIANT UnconditionalAppendNeverRejected
