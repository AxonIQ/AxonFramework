\* The same divergence seen by the primary oracle rather than by the
\* append-outcome oracle: the engine's verdict and the reference model's verdict
\* disagree, which is what a replayed history reports.
\* EXPECT: AppendConformsToDcbModel VIOLATED.
SPECIFICATION Spec

CONSTANTS
    Writers = {w1, w2}
    Events <- MCEvents
    Boundaries <- MCBoundaries
    Batches <- MCBatches
    MaxLen = 3
    INFINITY_IS_ORIGIN = TRUE

INVARIANT TypeOK
INVARIANT AppendConformsToDcbModel
