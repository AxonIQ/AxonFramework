= Axon Hunt -- Findings
:toc: left
:toclevels: 2
:icons: font

Living log of everything the hunt suite has found in Axon Framework 5. Entries are numbered
`F-<n>` and never renumbered.

[NOTE]
====
*The engine is never patched by this suite.* A confirmed defect gets an entry here with a candidate
fix, plus an expected-gap test that passes while the gap exists and flips red when it is closed. The
fix itself is a separate, reviewed change made by whoever owns the code.
====

Every entry carries how it was found. A finding discovered *by reading* is legitimate and is
labelled as such; it is weaker evidence than one a failing test produced, and saying so is the point
of the label.

Severity scale: *critical* (silent data loss or corruption in a documented configuration), *high*
(a documented guarantee does not hold), *medium* (a real defect with a narrow blast radius or an
easy workaround), *low* (a documentation or ergonomics gap).

== Reproduction state -- what each finding can actually be re-checked with

A named test *existing* is not the same as that test *asserting* the finding. F-19 was exactly that failure: it named an
arm that runs, prints `axonserver:FAIL`, and asserts nothing about that verdict. Every entry below is therefore audited
into one of three states, and no entry's status line may claim more than its state supports.

[horizontal]
*(a) asserted*:: A committed test contains an assertion that goes red when the behaviour changes. Expected-gap form: it
documents what the framework does *today* and is documented to flip when the gap closes. The preferred state.
*(b) probed*:: The effect is rate-based, licensed, or environment-dependent, and an assertion on its presence would be
red or green by luck. A committed diagnostic reports its numbers every run and asserts only what one run can decide.
Honest where (a) would be a lie.
*(c) inspected*:: No runtime reproduction. Correct for a finding about what the code *says* rather than what it does, and
then it must carry a pastable inspection command -- a `grep`, a `javap`, a `sed` on named lines -- that shows the defect in
seconds. F-1 is the model.

[cols="1,1,3,2"]
|===
| Finding | State | What re-checks it | Runs without Docker?

| F-1 | *(c)* | Two greps, printing both sides of the swap: the Spring Boot `@DefaultValue`s and the core defaults | yes
| F-2 | *(c)* | One grep over `InMemoryTokenStore`. Weaker than F-1: shows the implementation, not the contract it contradicts | yes
| F-3 | *(a)* | `PartialBatchVisibilityTest:90` -- `assertThat(torn)...isNotEmpty()`, where a `POLL` note is recorded only for a read that saw part of a batch | yes
| F-4 | *(c)* | Two greps over the claim-expiry clock and the injected-clock contrast site | yes
| F-5 | *(c)* | One grep over the hardcoded post-split block | yes
| F-6 | *(a)* for the identifier collapse; the segment starvation is unpinned | `SequencingPolicyOrderTest:97` -- `resolvedSequenceKeys(history)).hasSize(1)` across six streams | yes
| F-7 | *(a)* | `SequencingPolicyOrderTest` -- the `NoSuchElementException` note, `deliveredEvents` zero, and the delivery invariant FAIL. Three independent ways to go red | yes
| F-8 | *(a)* | `TransactionPhaseFailureTest:163-168` -- a rollback was requested, and it carries `afterCommit=true` | yes
| F-9 | *(a)* | `ClaimCapableBackendTest:149` -- `assertThat(identifierFailures.get()).isPositive()` | yes
| F-10 | *(a)* | `SegmentOwnershipUnderSkewTest` -- three assertions, one per measured bound, including `ownershipViolations).isNotEmpty()` | yes
| F-11 | *(b)* | `SplitAndMergeUnderLoadTest` asserts `violations()).isEmpty()` -- the redelivery is licensed, so the arm passes whether or not the finding holds | yes
| F-12 | *(c)* | Two greps. **The named arm passes**: the harness supplies the converter before the defect can fire | yes
| F-13 | *(c)* | One grep. Not a defect; recorded as a harness constraint | yes
| F-14 | *(a)* | `BackendDifferentialTest:272-277` -- the PostgreSQL arm must carry `UnconditionalAppendNeverRejected`, and the in-heap arm must be clean | **no** (container)
| F-15 | *(b)* | Two inherited integration-test suites that currently fail. They assert the *guarantee*, so closing the gap makes them pass -- they do not pin the finding | **no** (container, and `-Pintegration-test`)
| F-16 | *(b)* | `StoreInfrastructureFailureTest` prints `missing=` / `lossViolations=` every run; the loss assertion is inside `if (stalled && missing > 0)` | **no** (container)
| F-17 | *(c)* | One grep showing the store's `commit()` is a documented no-op. The measurement itself needs a source revert of `LateCommitFault` | **no** (container)
| F-18 | *(a)*, twice | `ConnectorCompatibilityTest:305` -- the shimmed method must still be one the connector lacks; and `AxonServerBackendTest:104` asserts the `AbstractMethodError` itself | **the first, yes**
| F-19 | *(b)* | `AppendBoundaryEnforcementUnderContentionTest` -- three constructions, reports its numbers, asserts only decidables. The suite arm reports the violation; the probe measures zero | **no** (container)
| F-20 | *(a)* | `AxonServerInfrastructureFailureTest` -- the append-failure classification set must be exactly `{AppendEventsTransactionRejectedException}` under eight landed cuts | **no** (container)
| F-21 | *(a)* | `DurableProgressMonotonicityTest:130` and `:163`, against the `:207` control that asserts the guarantee | yes
| F-22 | *(a)* | `BroadcastSentinelCollisionTest:109` -- a sentinel-valued key is handled in every segment, against the `:106` one-segment control | yes
| F-23 | *(c)* | A grep plus a `sed` on named lines, printing both sides of the mismatch | yes
|===

*Eleven of the twenty-three are asserted, four are probed, eight are inspected.* Six of the eight inspected are not a
backlog: F-1, F-2, F-4, F-5, F-13 and F-23 are findings about what the code and its documentation *say*, and a test
asserting a default value would restate the constant rather than check anything. F-12 and F-17 are the two where (c) is a
concession rather than the right answer, and both say so in their own entries.

Eight of the eleven asserted states run in the default build with no Docker. The container-tagged ones -- F-14, F-19,
F-20, and the second half of F-18 -- are behind `-Dhunt.excludedGroups=fuzz`, which is what a nightly job clears.

== At a glance

[cols="1,3,1,1"]
|===
| Finding | In plain terms | Severity | Status

| *F-1* `SpringBootInvertsGapSettings`
| Under Spring Boot autoconfigure the JPA event store gets a 10-second gap timeout and a 60000-entry
  gap window, where the core defaults are 60 seconds and 10000. The two values are swapped.
| High
| Confirmed by reading. Open.

| *F-2* `InMemoryTokenStoreHasNoOwnership`
| `InMemoryTokenStore` implements no claim or ownership at all, and its Javadoc does not warn about
  it. Any test asserting claim semantics against it passes vacuously.
| Medium
| Confirmed by reading. Open, documented as a harness constraint.

| *F-3* `PartialBatchVisibleDuringCommit`
| A multi-event batch is not made visible atomically. A concurrent reader can observe a strict prefix
  of a batch that is still committing.
| Medium
| *Reproduced by test.* Open.

| *F-4* `ClaimExpiryReadsGlobalClock`
| Token-claim expiry is evaluated against a JVM-global clock, not a per-node injected one, so
  per-node clock skew cannot be simulated inside a single JVM.
| Low
| Confirmed by reading. Open, recorded as a harness constraint.

| *F-5* `SplitBlocksReclaimForAHardcodedMinute`
| A segment split blocks local re-claim of the segment for a hardcoded 60 seconds, not configurable
  and not scaled by any timescale setting.
| Low
| Confirmed by reading. Open.

| *F-6* `DefaultSequencingPolicyDegradesOnDcbStores`
| The wired default sequencing policy resolves from a resource that DCB-native stores never set, so
  on every DCB backend the default silently degrades to fully sequential single-key processing. The
  Javadoc names a different default.
| Medium
| *Reproduced by test*, and sharper than first written: every event lands in one segment, so the
  configured segment count buys nothing either. Open.

| *F-7* `EmptySequenceIdentifierThrows`
| `SimpleEventHandlingComponent` unwraps the sequencing policy's `Optional` with `get()`. Choosing
  `NoOpSequencingPolicy`, or `SequentialPerAggregatePolicy` on a DCB store, therefore throws
  `NoSuchElementException` on every event and the processor delivers nothing at all.
| High
| Reproduced by test. Open.

| *F-8* `RollbackAfterCommitIsUnspecified`
| The framework calls `AppendTransaction.rollback()` from a single per-transaction error handler
  whatever phase the error arrived in, so an error after a successful commit rolls back an already
  published batch. The contract does not say what that means.
| Low
| Reproduced by test. Open.

| *F-9* `ConcurrentStartupLosesInstances`
| Several processors starting at the same instant against a shared JDBC token store race to create the store's
  identifier row. One wins; the rest get a primary-key violation that the store turns into a startup failure instead
  of re-reading the row that now exists, so all but one instance fails to start. Measured: 3 of 4.
| High
| *Reproduced by test.* Open.

| *F-10* `ClaimProtocolToleratesNoStatedClockSkew`
| The claim protocol compares a timestamp one node wrote against another node's clock and states no tolerance for the
  two disagreeing. Measured: a node whose clock reads ahead takes a live claim up to that far early, so two nodes hold
  one segment for up to the skew, capped by one claim timeout. Nothing warns, and nothing in the protocol notices.
  Model-checked, and the model overturned the third measured bound: the margin between the claim timeout and the
  refresh rate is not a tolerated skew, only the skew a punctual owner happens to hide.
| Medium
| *Reproduced by test*, quantified, and *model-checked*. Open.

| *F-11* `MergeRewindsToTheLowerOfTwoTokens`
| Merging two segments gives the merged one the lower of the two halves' tokens, so every event the further-ahead half
  had already handled is delivered again. The behaviour is deliberate and the documentation does not mention it.
| Medium
| *Reproduced by test.* Open.

| *F-12* `ResetDemandsAConverterItNeverUses`
| Rewinding a processor's tokens resolves a `GeneralConverter` out of the processing context in order to convert the
  reset context -- and does so even when there is no reset context. A processor wired over an application context that
  provides no components therefore cannot be reset at all, and fails with an error that says nothing about resets.
| Low
| *Reproduced by test.* Open.

| *F-15* `AggregateMarkerLowerBoundUnimplemented`
| Sourcing twice in one processing context on the aggregate-based JPA event store throws
  `UnsupportedOperationException: Not implemented yet`. The framework combines the two consistency markers with
  `lowerBound`, and the aggregate-based marker does not implement it. Ordinary event-sourced command handling reaches it.
| High
| *Reproduced by test*, on PostgreSQL, with a per-backend verdict vector. Open.

| *F-14* `UnconditionalAppendRejectedOnAggregateStore`
| An append made with no consistency condition is rejected as conflicting on the aggregate-based JPA event store
  whenever the tagged aggregate already holds events. The framework documents such an append as having conflict
  detection disabled entirely; on this engine it succeeds exactly once per aggregate and is refused for ever after, with
  an exception that tells the caller to retry. Model-checked as a violated/fixed pair, which also settles that the
  divergence is over-rejection only: at the model's bounds the collapsed marker never accepts an append the protocol
  forbids.
| High
| *Reproduced by test*, on PostgreSQL, with a per-backend verdict vector, and *model-checked*. Open.

| *F-13* `AStalledHandlerKeepsItsClaims`
| A node frozen inside its event handler does not lose its segments: extending a claim is the coordinator's work, on a
  separate thread that keeps running. The classic stalled-owner failure therefore needs the whole process to stall, not
  just its handling, which is worth knowing before anybody writes a test for it.
| Low (not a defect)
| Measured. Recorded as a harness constraint.

| *F-16* `EventSkippedWhenItsTimestampAgesPastTheGapTimeout`
| An event whose transaction commits later than `gapTimeout` after the event's own message timestamp can be skipped by a
  streaming reader for ever. The reader decides whether to record a gap by comparing the event it can see against the
  wall clock, so a neighbour that has aged past the threshold suppresses the gap for the hole below it, and nothing ever
  asks for that index again.
| High
| *Reproduced by test*, on PostgreSQL, on both configuration paths. Open.

| *F-17* `DatabaseTransactionCommitRacesAppendTransactionCommit`
| On a store whose transaction lives on the processing context, the database transaction's commit and
  `EventStorageEngine.AppendTransaction.commit()` are two actions registered in the same lifecycle phase, and the
  framework runs same-phase actions with no ordering between them. An append's events were measured readable 1877ms
  before the `commit()` of the append that produced them was entered. Nothing in the interface says that `commit()` is
  not the durability point.
| Medium
| *Measured.* Open.

| *F-18* `ReleasedConnectorMissesAnAbstractStorageEngineMethod`
| Every published Axon Server connector -- 5.1.2, 5.2.1 and 5.2.2 alike -- implements only
  `EventStorageEngine.source(SourcingCondition)`, while this reactor made
  `source(SourcingCondition, ProcessingContext)` abstract. `javac` accepts a call, the JVM refuses it with
  `AbstractMethodError`. An undetected binary-compatibility break between the framework and its own released client
  library, not a missing artefact.
| Medium (a released client library cannot be loaded)
| *Measured.* No longer blocking: the arm runs with that one method supplied by the harness, and a
  compatibility gate now names the set in seconds.

| *F-19* `AxonServerAcceptsAnAppendItsBoundaryForbids`
| On the suite's path, an append is accepted although an event matching its consistency boundary was committed at or
  after its consistency marker before the append's own commit. The first violation is always an append carrying
  `ConsistencyMarker.ORIGIN` -- "no event inside this boundary exists anywhere" -- and the rate is one per run, of about
  72 such appends. The in-heap engine, on the identical scenario, does not. But a probe driving the published connector
  directly does not reproduce it either, in three constructions over fourteen runs, so *whose* defect it is is open.
| High (*if real*; the attribution is open)
| *(b) probed.* The suite oracle reports it on demand; the committed probe measures zero. Mechanism and attribution
  both open. Open.

| *F-20* `ConnectorReportsEveryCommitFailureAsAConsistencyRejection`
| The Axon Server connector wraps *every* throwable from an append commit in
  `AppendEventsTransactionRejectedException` and drops the cause, so a dropped connection is indistinguishable from the
  store deciding against the append. Measured: 191 of 191 failures under a network partition classified as definite
  rejections, none ambiguous, 18 of them on appends that carry no consistency condition at all and therefore cannot
  conflict.
| High
| *Confirmed in the connector's bytecode and reproduced by test.* Open.
|===

== F-1 -- Spring Boot autoconfigure inverts `gapTimeout` and `maxGapOffset`

*Severity:* High +
*Status:* Open. Confirmed by reading; no test exists yet. +
*Claims:* C14, M12 +
*Backend verdict vector:* `postgres-jpa:AFFECTED (Spring Boot path only)`, `in-memory:N/A`,
`axonserver:N/A`

=== What is wrong

The JPA event storage engine has two independent gap settings. `gapTimeout` is a duration in
milliseconds after which a recorded gap is dropped from a tracking token. `maxGapOffset` is a count:
how many positions ahead of a newly-seen index may be recorded as gaps.

The core configuration defaults them to 60000 ms and 10000 entries. The Spring Boot autoconfigure
properties default them to 10000 ms and 60000 entries. The two values are swapped, and the swapped
values are applied unconditionally.

=== Evidence

Core defaults:

* `eventsourcing/src/main/java/org/axonframework/eventsourcing/eventstore/jpa/AggregateBasedJpaEventStorageEngineConfiguration.java:84` -- `DEFAULT_MAX_GAP_OFFSET = 10000`
* `eventsourcing/src/main/java/org/axonframework/eventsourcing/eventstore/jpa/AggregateBasedJpaEventStorageEngineConfiguration.java:86` -- `DEFAULT_GAP_TIMEOUT = 60000`

Spring Boot defaults:

* `extensions/spring/spring-boot-autoconfigure/src/main/java/org/axonframework/extension/springboot/JpaEventStorageEngineConfigurationProperties.java:44` -- `@DefaultValue("10000") int gapTimeout`
* `extensions/spring/spring-boot-autoconfigure/src/main/java/org/axonframework/extension/springboot/JpaEventStorageEngineConfigurationProperties.java:46` -- `@DefaultValue("60000") int maxGapOffset`

Applied unconditionally:

* `extensions/spring/spring-boot-autoconfigure/src/main/java/org/axonframework/extension/springboot/autoconfig/JpaEventStoreAutoConfiguration.java:107` -- `.gapTimeout(properties.gapTimeout())`
* `extensions/spring/spring-boot-autoconfigure/src/main/java/org/axonframework/extension/springboot/autoconfig/JpaEventStoreAutoConfiguration.java:109` -- `.maxGapOffset(properties.maxGapOffset())`

=== Why it matters

The gap timeout is the window a not-yet-committed event has to become visible before a streaming
consumer stops looking for it. Shrinking it from 60 seconds to 10 makes it six times more likely
that a transaction outliving the window commits an event that is then never streamed to any
consumer, which is silent event loss on the read side. Widening `maxGapOffset` from 10000 to 60000
enlarges every tracking token by up to six times, which is a throughput and token-size cost rather
than a correctness one.

The direction of the error is the bad one: the setting that got smaller is the one guarding against
loss. And it applies to the configuration path most production applications use, since nearly every
Axon application on JPA is a Spring Boot application.

=== How it was found

By reading, during the claims-mining pass that preceded any test. The claim being verified was C14
("a gap older than `gapTimeout` is dropped from the token"), and checking the default value through
both configuration paths surfaced the disagreement. No test in the repository asserts that the two
paths produce the same engine.

=== Candidate fix

Swap the two `@DefaultValue` annotations in `JpaEventStorageEngineConfigurationProperties`, so that
`gapTimeout` defaults to `60000` and `maxGapOffset` to `10000`, matching the core configuration
record. One-line change on each of two lines; no behavioural change for anyone who set the
properties explicitly.

A durable guard is worth more than the fix: a test that builds the engine through both configuration
paths and asserts every effective setting is pairwise equal, or is listed in an explicit allow-list
of deliberate differences. That catches this whole class of silent configuration drift rather than
this one instance. Planned as scenario S19.

=== Reproduce

No automated reproduction yet. By inspection:

[source,bash]
----
grep -n 'DefaultValue' extensions/spring/spring-boot-autoconfigure/src/main/java/org/axonframework/extension/springboot/JpaEventStorageEngineConfigurationProperties.java
grep -n 'DEFAULT_GAP_TIMEOUT\|DEFAULT_MAX_GAP_OFFSET' eventsourcing/src/main/java/org/axonframework/eventsourcing/eventstore/jpa/AggregateBasedJpaEventStorageEngineConfiguration.java
----

== F-2 -- `InMemoryTokenStore` implements no ownership, and does not say so

*Severity:* Medium +
*Status:* Open. Confirmed by reading. +
*Claims:* C18, C19, C20, C21, C22, M11

=== What is wrong

The `TokenStore` interface documents that fetching a token claims it for the current process,
"preventing access by other instances", that a claim may be taken only when the entry is unowned,
owned by the same node, or expired, and that extending a lost claim fails. `InMemoryTokenStore`
implements none of it. There is no owner field, no timestamp, no expiry.

=== Evidence

* `messaging/src/main/java/org/axonframework/messaging/eventhandling/processing/streaming/token/store/inmemory/InMemoryTokenStore.java:126` -- `fetchToken` never fails on ownership
* `messaging/src/main/java/org/axonframework/messaging/eventhandling/processing/streaming/token/store/inmemory/InMemoryTokenStore.java:140` -- `releaseClaim` is a documented no-op
* `messaging/src/main/java/org/axonframework/messaging/eventhandling/processing/streaming/token/store/inmemory/InMemoryTokenStore.java:191` -- `fetchAvailableSegments` returns every segment, claimed or not
* `messaging/src/main/java/org/axonframework/messaging/eventhandling/processing/streaming/token/store/inmemory/InMemoryTokenStore.java:50` -- the class Javadoc says only "thread-safe"

=== Why it matters

For the framework's users this is defensible: an in-memory store is not shared between processes, so
there is nothing to arbitrate. The defect is that the class does not say so. A reader comparing the
interface's contract against this implementation has no warning that five documented guarantees
simply do not apply.

For this suite it is a hard constraint. Any ownership assertion made against `InMemoryTokenStore`
passes vacuously, which is worse than no test at all, because it looks like coverage. Every
ownership scenario must run against a claim-capable store.

=== How it was found

By reading, while checking whether the planned multi-node layer could be built on the in-memory
store. It cannot.

=== Candidate fix

Documentation, not code. Add to the class Javadoc that this implementation provides no ownership
arbitration and that the claim-related guarantees of `TokenStore` do not hold for it, with a pointer
to the JDBC and JPA stores for any multi-process use.

=== Reproduce

[source,bash]
----
grep -n 'releaseClaim\|fetchAvailableSegments\|fetchToken' messaging/src/main/java/org/axonframework/messaging/eventhandling/processing/streaming/token/store/inmemory/InMemoryTokenStore.java
----

== F-3 -- A committing batch is observable in part

*Severity:* Medium +
*Status:* Open. *Reproduced by test.* +
*Claims:* C9, C4, M17

=== What is wrong

`EventStorageEngine` documents that events "may only be visible to consumers after the invocation of
`commit()`". `InMemoryEventStorageEngine.commit()` inserts the batch one event at a time while
holding an append lock, but readers traverse the storage map without taking that lock. A reader
polling during a commit can therefore observe a strict prefix of a batch that has not finished
committing.

The documented guarantee is about the boundary between before-commit and after-commit. It says
nothing about atomicity *within* the commit, and the implementation does not provide it.

=== Evidence

* `eventsourcing/src/main/java/org/axonframework/eventsourcing/eventstore/inmemory/InMemoryEventStorageEngine.java:127-146` -- per-event `eventStorage.put(next, event)` inside `appendLock`
* `eventsourcing/src/main/java/org/axonframework/eventsourcing/eventstore/inmemory/InMemoryEventStorageEngine.java:290-306` -- `next()` reads `eventStorage.containsKey` with no lock
* `eventsourcing/src/main/java/org/axonframework/eventsourcing/eventstore/EventStorageEngine.java:187` -- the guarantee, which is weaker than batch atomicity

=== Why it matters

A projection that assumes a multi-event transaction arrives whole will see a torn state. Whether
that matters depends on the projection, but the framework gives no way to tell that a batch is
incomplete, so a consumer cannot defend against it.

=== How it was found

By reading, during the claims-mining pass. The `DcbStoreModel` reference model does not reproduce
it, and cannot: the model is sequential, so it has no notion of a reader observing an operation
mid-flight. `ModelAndInMemoryEngineAgreeTest` applies operations one at a time to both sides and is
therefore structurally unable to see this divergence. That is a stated boundary of the model rather
than a gap in it, and it is why the finding is not yet reproduced.

=== Candidate fix

Two shapes are plausible, and choosing between them is a design decision for the framework's owners
rather than for this suite. Either publish the batch under a single visible-position advance, so
readers see a position boundary rather than individual map entries, or state explicitly in
`EventStorageEngine`'s contract that a batch is not atomically visible and that consumers must not
assume it is.

=== What the reproduction observed

The scenario `partial_batch_never_visible_to_concurrent_reader` puts one writer committing
hundred-event batches next to one reader reading the whole store as fast as it can, and counts, per
single read, how many of each batch were visible in that read. A count strictly between one and a
hundred is a batch caught half-written.

Five consecutive runs of the arm, on the same machine, one after another:

[source]
----
run 1  polls=8707  torn observations=12
run 2  polls=6410  torn observations=32
run 3  polls=4904  torn observations=15
run 4  polls=7261  torn observations=19
run 5  polls=7343  torn observations=20
----

Sample observations from one run, as recorded in the history under the `poll` operation:

[source]
----
{observed={b0=1},  batchSize=100}
{observed={b1=1},  batchSize=100}
{observed={b1=41}, batchSize=100}
----

Inside a full run of the suite, where the JVM is warm and the writer is faster, the rate holds but
the absolute count is lower -- 9 torn observations out of 5310 reads across the arm's three seeds.
The arm runs three seeds rather than one for exactly that reason: roughly one read in four hundred
catches a batch half-written, so a single run is too thin a sample to depend on.

A reader that could see event `b1-e0` through `b1-e40` and nothing beyond, while the writer was
still inserting `b1-e41` onwards, is precisely the strict prefix the finding predicted. The
mechanism is confirmed by the code path as well: `source(...)` snapshots the stream's end position
from `eventStorage.lastKey()` at the moment the stream is opened, so a stream opened mid-commit ends
at whatever position the writer had reached.

Two neighbouring facts were asserted alongside, so the finding is not read as stronger than it is.
Nothing is delivered before its commit -- `NoVisibilityBeforeCommit` held on every run -- and the
tear is transient: the authoritative scan taken after the run had quiesced held every batch whole,
one hundred events each, with none missing. The gap is in the atomicity of a batch's *arrival*, not
in what the store ends up holding.

=== Reproduce

[source,bash]
----
./mvnw -Phunt -pl simulation -am test -Dtest=PartialBatchVisibilityTest \
    -Dsurefire.failIfNoSpecifiedTests=false
----

The test asserts that a torn observation *is* found, because the suite records the framework's
current behaviour and never patches it. It is documented to flip red the day batch visibility
becomes atomic, and that flip is the signal to re-evaluate this entry and turn the assertion around.

== F-4 -- Token-claim expiry reads a JVM-global clock

*Severity:* Low +
*Status:* Open. Confirmed by reading. +
*Claims:* C20, M3

=== What is wrong

Whether a token claim has expired is decided by comparing a stored timestamp against
`ClockUtils.instant()`, which reads a single static `AtomicReference<Clock>` shared by the whole
JVM. There is no per-node clock on that path, even though other parts of the processor take an
injected `Clock`.

=== Evidence

* `common/src/main/java/org/axonframework/common/ClockUtils.java:49` -- `private static final AtomicReference<Clock> CLOCK`
* `messaging/src/main/java/org/axonframework/messaging/eventhandling/processing/streaming/token/store/jpa/TokenEntry.java:159-160` -- `expired()` reads `ClockUtils.instant()`
* `messaging/src/main/java/org/axonframework/messaging/eventhandling/processing/streaming/pooled/SplitTask.java:113` -- by contrast, uses an injected `clock` field

=== Why it matters

For the framework, this is a documentation gap: nothing states what clock skew between nodes the
claim protocol tolerates, and the expiry check compares a remotely-written timestamp against the
local clock with no stated allowance.

For this suite it is a determinism-seam constraint. Two "nodes" in one JVM cannot be given different
clocks through this path, so any scenario that needs per-node skew must either use a claim-aware
fake token store whose expiry reads an injected clock, or run the nodes in separate processes.

=== How it was found

By reading, while mapping the injectable seams available for the multi-node layer.

=== Candidate fix

Two independent improvements. Document the tolerated clock skew for the claim protocol, since a
protocol that compares timestamps written by different machines needs a stated bound. Separately,
route the expiry check through an injectable clock so that skew is testable at all; that is a wider
change than this suite should propose without the framework's owners weighing in.

=== Reproduce

[source,bash]
----
grep -n 'ClockUtils.instant' messaging/src/main/java/org/axonframework/messaging/eventhandling/processing/streaming/token/store/jpa/TokenEntry.java
grep -n 'AtomicReference<Clock>' common/src/main/java/org/axonframework/common/ClockUtils.java
----

== F-5 -- A split blocks re-claim for a hardcoded minute

*Severity:* Low +
*Status:* Open. Confirmed by reading. +
*Claims:* M10

=== What is wrong

When a processor splits a segment, it blocks its own coordinator from re-claiming that segment for
60 seconds. The duration is a literal in the code, not a configuration setting, and it does not
scale with any of the processor's other timing settings.

=== Evidence

* `messaging/src/main/java/org/axonframework/messaging/eventhandling/processing/streaming/pooled/SplitTask.java:113` -- `clock.instant().plusSeconds(60)`

The guard is also stored in a per-coordinator map, so it constrains only the splitting node; nothing
stops another node claiming the segment mid-split.

=== Correction, found by reading the same file more carefully while budgeting the split scenario

The sixty seconds is a ceiling, not the duration. `SplitTask.splitAndRelease` removes the segment from
`releasesDeadlines` in a `whenComplete` block, so a split that completes clears its own guard
(`messaging/src/main/java/org/axonframework/messaging/eventhandling/processing/streaming/pooled/SplitTask.java:141-143`),
and `MergeTask` does the same for both halves (`MergeTask.java:177-178`). The block therefore lasts as long as the
instruction takes, and the minute only applies to a split that never finishes. The operational impact is far smaller
than this entry originally claimed, and the measured consequence for a time-compressed suite is a delay of hundreds of
milliseconds rather than a minute: the split-and-merge storm ran seven splits and seven merges per seed inside a
four-second window with no segment left unclaimable. What remains true is that the duration is a literal, that it does
not scale with any other setting, and that the guard is per-coordinator.

=== Why it matters

Two consequences. Operationally, a split costs a minute of unavailability for that segment on the
splitting node regardless of how fast the split actually completed. For this suite, any scenario
running with compressed timeouts has a 60-second term that does not compress, which will look like a
liveness violation to a naive horizon.

=== How it was found

By reading, while budgeting the split and merge scenarios.

=== Candidate fix

Derive the block from `tokenClaimInterval` (or make it a configuration setting defaulting to the
current value) so that a time-compressed configuration compresses it too. Separately, the per-node
scope of the guard is worth documenting, since it reads as a global exclusion and is not one.

=== Reproduce

[source,bash]
----
grep -n 'plusSeconds(60)' messaging/src/main/java/org/axonframework/messaging/eventhandling/processing/streaming/pooled/SplitTask.java
----

== F-6 -- The wired default sequencing policy does not match the documented one, and degrades on DCB stores

*Severity:* Medium +
*Status:* Open. *Reproduced by test*, in part: the collapse to one sequence identifier is asserted, the segment starvation
it causes is not. +
*Reproduction state:* *(a) asserted*, for the identifier. `SequencingPolicyOrderTest` asserts
`resolvedSequenceKeys(history)).hasSize(1)` across six independent streams, which is exactly what the wired default
collapsing to `SequentialPolicy` produces; the day `SequentialPerAggregatePolicy` resolves on a DCB store it becomes six
and the assertion goes red. *The sharper consequence below -- that three of the four segments receive nothing at all --
is asserted nowhere and printed nowhere*, and is therefore a measurement taken once rather than a pinned one. +
*Claims:* C32, M14

=== What is wrong

`SequencingPolicy`'s Javadoc names `SequentialPerAggregatePolicy` as the default event-processing
policy. The wired default is a hierarchical policy that tries `SequentialPerAggregatePolicy` first
and falls back to `SequentialPolicy`. `SequentialPerAggregatePolicy` resolves its key from a legacy
aggregate-identifier resource, which DCB-native stores never populate. On every DCB backend the
fallback therefore always wins, and the effective default is fully sequential processing of the
whole segment under a single key.

=== Evidence

* `messaging/src/main/java/org/axonframework/messaging/core/sequencing/SequencingPolicy.java:34-35` -- the documented default
* `messaging/src/main/java/org/axonframework/messaging/eventhandling/SimpleEventHandlingComponent.java:61-64` -- the wired default
* `messaging/src/main/java/org/axonframework/messaging/core/sequencing/SequentialPerAggregatePolicy.java:47` -- resolves from `LegacyResources.AGGREGATE_IDENTIFIER_KEY`

=== Why it matters

A user who reads the Javadoc expects per-aggregate parallelism within a segment and gets none. On a
DCB backend the same application processes its segment strictly one event at a time. That is safe,
and it is a throughput cliff nobody was told about. The behaviour also differs between the
aggregate-based JPA backend, where the legacy resource is populated, and every DCB backend, where it
is not, so the same configuration behaves differently depending on the store.

=== How it was found

By reading, while designing the ordering scenarios. It changed the design: the planned "empty policy
means parallel" arm was testing a policy that is not the default.

=== Candidate fix

At minimum, correct the Javadoc on `SequencingPolicy` to name the hierarchical policy that is
actually wired, and state that its per-aggregate half is inert on stores that do not populate the
aggregate-identifier resource. The larger question, whether DCB-native stores should populate an
equivalent resource so that per-key parallelism is available by default, is a design decision for
the framework's owners.

=== What the reproduction observed, and the part that was missed by reading

The scenario `sequencing_policy_order_preserved_wired_default` writes one hundred and eighty events
across six independent streams, with four segments and four workers configured, and records the
sequence identifier the framework resolved for every delivery. The run's read-model state:

[source]
----
{"unresolvedSequenceKeys": 0, "deliveredEvents": 180, "arm": "wired-default", "distinctSequenceKeys": 1}
----

One distinct identifier for one hundred and eighty events over six streams. The ordering oracle
`SequenceKeyOrderPreserved` held, which it must: with one identifier, per-key order is global order.

The consequence found by testing rather than by reading is worse than the entry originally said. A
segment is chosen by hashing the sequence identifier
(`MSG/.../segmenting/SegmentMatcher.java:65`, reached from
`MSG/eventhandling/processing/ProcessorEventHandlingComponents.java:193-198`), so one identifier
means one hash, which means **one segment**. Configuring four segments and four workers on a
DCB-native store does not merely fail to parallelise within a segment; three of the four segments
receive nothing at all. The throughput cliff is a factor of the segment count, not a factor of one.

=== Reproduce

[source,bash]
----
./mvnw -Phunt -pl simulation -am test -Dtest=SequencingPolicyOrderTest \
    -Dsurefire.failIfNoSpecifiedTests=false
----

By inspection:

[source,bash]
----
grep -n 'Default event processing policy' messaging/src/main/java/org/axonframework/messaging/core/sequencing/SequencingPolicy.java
sed -n '55,70p' messaging/src/main/java/org/axonframework/messaging/eventhandling/SimpleEventHandlingComponent.java
grep -n 'AGGREGATE_IDENTIFIER_KEY' messaging/src/main/java/org/axonframework/messaging/core/sequencing/SequentialPerAggregatePolicy.java
----

== F-7 -- An unresolvable sequence identifier throws, and stops the read side dead

*Severity:* High +
*Status:* Open. Reproduced by test. +
*Claims:* C32, C33, C34, M14

=== What is wrong

`SequencingPolicy.sequenceIdentifierFor` returns an `Optional`, and its own Javadoc says an empty
result means the policy cannot determine an identifier and that "it is up to the component using
this policy to provide a default behavior, use another policy, throw an exception or react in any
other way -- as appropriate".

`SimpleEventHandlingComponent` unwraps it with `get()`. Two policies the framework ships return an
empty `Optional` in ordinary use, and both therefore make every single event throw
`NoSuchElementException` before it can be handled:

* `NoOpSequencingPolicy`, whose entire purpose is to impose no sequencing, and whose Javadoc says
  infrastructure "may decide upon this sequencing policy being present bypassing sequencing
  infrastructure at all";
* `SequentialPerAggregatePolicy` used on its own against any store that does not populate
  `LegacyResources.AGGREGATE_IDENTIFIER_KEY`, which is every DCB-native store (finding F-6).

The framework contains the graceful handling of the same situation a few classes away.
`SegmentMatcher.sequenceIdentifier` resolves the identical `Optional` with
`.orElseGet(event::identifier)`. Two components, one situation, opposite answers.

=== Evidence

* `messaging/src/main/java/org/axonframework/messaging/eventhandling/SimpleEventHandlingComponent.java:169-185` -- `@SuppressWarnings("OptionalGetWithoutIsPresent")` over two `get()` calls
* `messaging/src/main/java/org/axonframework/messaging/eventhandling/SimpleEventHandlingComponent.java:176` -- `return sequencingPolicy.sequenceIdentifierFor(event, context).get();`
* `messaging/src/main/java/org/axonframework/messaging/eventhandling/SimpleEventHandlingComponent.java:184` -- `.orElse(sequencingPolicy.sequenceIdentifierFor(event, context).get())`
* `messaging/src/main/java/org/axonframework/messaging/core/sequencing/NoOpSequencingPolicy.java:47-50` -- returns `Optional.empty()`
* `messaging/src/main/java/org/axonframework/messaging/core/sequencing/SequentialPerAggregatePolicy.java:47` -- returns `Optional.ofNullable(context.getResource(LegacyResources.AGGREGATE_IDENTIFIER_KEY))`
* `messaging/src/main/java/org/axonframework/messaging/core/sequencing/SequencingPolicy.java:76-81` -- the contract the `get()` breaks
* `messaging/src/main/java/org/axonframework/messaging/eventhandling/processing/streaming/segmenting/SegmentMatcher.java:78` -- the same resolution, handled gracefully

Line 184 carries a second, independent defect. `Optional.orElse` evaluates its argument eagerly, so
the fallback `get()` runs even when a sub-component did resolve an identifier. A component whose
handlers include another `EventHandlingComponent` therefore throws anyway, although the value it
would have returned was available.

=== Why it matters

It is silent and total. There is no configuration error, no startup failure and no rejected
registration: the processor starts, claims its segments, and then throws on every event for ever.
One measured run threw two thousand two hundred and four times in eight seconds and delivered
nothing, because the coordinator retries a work package that failed.

`NoOpSequencingPolicy` is the more serious of the two, because choosing it is a deliberate,
documented act. A user who reads its Javadoc and selects it gets a projection that never advances.

=== How it was found

By testing. The scenario arms were written from the plan's expectation that these policies would
yield "empty sequence identifiers" and that the run would exhibit unsequenced handling. What the
arms actually observed was no handling at all.

=== Candidate fix

Resolve the `Optional` rather than unwrap it. `SegmentMatcher` already shows the shape:
`.orElseGet(event::identifier)` gives every event its own identifier, which is the natural reading
of "no sequencing requirements for the handling of this message". Whatever default is chosen, both
call sites need it, and the eager `orElse` on the second one must become `orElseGet` so the fallback
is not evaluated when a sub-component answered.

A test asserting that every shipped `SequencingPolicy` can be handed to
`SimpleEventHandlingComponent.create` and produce a working processor would catch the whole class.

=== Reproduce

[source,bash]
----
./mvnw -Phunt -pl simulation -am test -Dtest=SequencingPolicyOrderTest \
    -Dsurefire.failIfNoSpecifiedTests=false
----

The `no-op` and `per-aggregate` arms both record `deliveredEvents: 0` and a `sequence` note carrying
`java.util.NoSuchElementException`, and both runs report `INCONCLUSIVE`: nothing was found broken,
and a read side that never ran cannot be reported as a pass.

== F-8 -- Rollback after a successful commit is unspecified, and is requested anyway

*Severity:* Low +
*Status:* Open. Reproduced by test. +
*Claims:* C29

=== What is wrong

`AppendTransaction.rollback()` is documented as rolling "back any events that have been appended,
permanently making them unavailable for consumers". Nothing says what it means once `commit()` has
succeeded, and no implementation can honour that wording at that point: the batch is published.

`DefaultEventStoreTransaction` registers exactly one error handler per transaction and calls
`rollback()` from it regardless of which phase the error arrived in. An error in the
`AFTER_COMMIT` phase -- which is where the transaction is asked for its consistency marker, strictly
after the commit -- therefore produces a rollback of a batch the store has already made visible.

=== Evidence

* `eventsourcing/src/main/java/org/axonframework/eventsourcing/eventstore/EventStorageEngine.java:204` -- the wording
* `eventsourcing/src/main/java/org/axonframework/eventsourcing/eventstore/EventStorageEngine.java:209-215` -- `afterCommit` is documented as called only after a successful commit
* `eventsourcing/src/main/java/org/axonframework/eventsourcing/eventstore/DefaultEventStoreTransaction.java:210-214` -- one `onError` handler, registered alongside the commit step, calling `tx.rollback()`
* `eventsourcing/src/main/java/org/axonframework/eventsourcing/eventstore/inmemory/InMemoryEventStorageEngine.java:157-160` -- the in-memory engine's `rollback()` only marks the transaction finished, so a post-commit rollback is a silent no-op

=== Why it matters

Mostly for whoever writes the next storage engine. The in-memory engine does the right thing by
accident: its `rollback()` cannot un-publish, so it does nothing. An engine that took the wording
literally and tried to delete the batch would destroy committed, already-delivered events because a
marker calculation failed.

It matters for this suite too, and that is how it was found. An oracle reading the rollback record
naively reports every committed event of such a transaction as observable-after-rollback, which is
a false finding with a very convincing message attached.

=== How it was found

By testing, while building the third arm of
`uncommitted_never_visible_rolledback_never_delivered`. The arm injects a failure in the
`AFTER_COMMIT` phase, and the first run reported a rolled-back transaction whose events were both
delivered and present in the store.

=== Candidate fix

Documentation first: state on `AppendTransaction.rollback()` that it is only meaningful before
`commit()` has completed, and that after a successful commit it must be a no-op. Then, optionally,
stop asking: `DefaultEventStoreTransaction` knows whether the commit completed, so its error handler
can skip the rollback once it has.

=== Reproduce

[source,bash]
----
./mvnw -Phunt -pl simulation -am test -Dtest=TransactionPhaseFailureTest \
    -Dsurefire.failIfNoSpecifiedTests=false
----

The after-commit arm asserts that a rollback was requested and carried `afterCommit=true`, and that
the committed events stayed in the store. The harness records such a rollback as having discarded
nothing, keeping the offered identifiers under `offeredEventIds`, which is what keeps
`RolledBackEventsNeverObservable` a real oracle instead of a source of false alarms.

== F-9 -- Starting several processors at once against a shared JDBC token store loses all but one

*Severity:* High +
*Status:* Open. *Reproduced by test.* +
*Claims:* C18, C36, M16 +
*Backend verdict vector:* `hsqldb-tokens:FAIL`, `in-memory:N/A (the in-heap token store keeps no identifier row)`

=== What is wrong

Starting a `PooledStreamingEventProcessor` begins by asking its token store for a storage identifier. A
`JdbcTokenStore` keeps that identifier in a row of the token table and creates it lazily, the first time anybody asks:
it selects the row, and inserts one if the select found nothing.

That read-then-write is not atomic and is not guarded. When several instances of an application start at the same
instant against the same token table, several of them select nothing and several of them insert the same primary key.
One insert wins. Every other gets a constraint violation, which `initializeConfigToken` wraps in an
`UnableToRetrieveIdentifierException` and rethrows -- without re-reading the row that, by then, definitely exists.
`PooledStreamingEventProcessor.start()` chains straight off that call, so the future it returns completes
exceptionally and the processor never starts.

The exception's own message is `"Exception while attempting to initialize the config token. It may have been
concurrently initialized."` The race was anticipated at the point where it is thrown and is still allowed to reach the
caller.

=== Evidence

* `messaging/src/main/java/org/axonframework/messaging/eventhandling/processing/streaming/token/store/jdbc/JdbcTokenStore.java:161-169` -- `retrieveStorageIdentifier` selects, then initialises when the select found nothing
* `messaging/src/main/java/org/axonframework/messaging/eventhandling/processing/streaming/token/store/jdbc/JdbcTokenStore.java:183-195` -- `initializeConfigToken` catches the `SQLException` and rethrows it as `UnableToRetrieveIdentifierException`, with no re-read
* `messaging/src/main/java/org/axonframework/messaging/eventhandling/processing/streaming/pooled/PooledStreamingEventProcessor.java:189-195` -- `start()` is `unitOfWork.executeWithResult(tokenStore::retrieveStorageIdentifier).thenAccept(...).thenCompose(...)`, so the failure fails the start
* `messaging/src/main/java/org/axonframework/messaging/eventhandling/processing/streaming/token/store/jpa/JpaTokenStore.java` -- the JPA store keeps the same config-token design, so the same race is expected there; not measured

=== Why it matters

This is the first-deploy and rolling-restart case, and it is exactly the deployment shape the token store exists for.
Bring four instances of an Axon application up together against a fresh database and three of them fail to start.
There is no retry, no warning and no degraded mode: `start()` completes exceptionally, and what happens next is
whatever the surrounding application does with a failed startup. A supervised deployment restarts the failed
processes and recovers on the second attempt, which is why this can go unnoticed in production and show up only as
"the first deploy always flaps".

It also blocks anything downstream of it from being tested. Because it serialises the boot, the segment-creation race
that follows never happens: one node comes up while the others are failing, and it has created every segment by the
time they retry.

=== How it was found

By testing. The scenario `concurrent_bootstrap_initializes_segments_exactly_once` boots four nodes from a barrier
against an empty store, and the first run of it did not reach its oracles at all -- three of the four processors
threw out of `start()`. The stack ended in an HSQLDB `SQLIntegrityConstraintViolationException` on `TOKENENTRY`.

=== Candidate fix

Re-read on conflict. `initializeConfigToken` already knows the insert failed because somebody else got there first;
selecting the row again and returning it is the whole fix, and it is three lines:

[source,java]
----
private ConfigToken initializeConfigToken(Connection connection) {
    try {
        return (ConfigToken) insertTokenEntry(connection, new ConfigToken(...), CONFIG_TOKEN_ID, CONFIG_SEGMENT);
    } catch (SQLException e) {
        ConfigToken concurrent = retrieveConfigToken(connection);
        if (concurrent != null) {
            return concurrent;
        }
        throw new UnableToRetrieveIdentifierException("...", e);
    }
}
----

The same treatment belongs on the JPA store. A durable guard is worth more than either: a test that starts N
processors simultaneously against one token store and asserts that all N come up.

=== Reproduce

[source,bash]
----
./mvnw -Phunt -pl simulation -am test -Dtest=ClaimCapableBackendTest \
    -Dsurefire.failIfNoSpecifiedTests=false
----

Measured output: `F-9: 3 of 4 simultaneous storage-identifier resolutions failed`.

The test asserts that at least one of the four fails, because the suite records the framework's current behaviour and
never patches it. It flips red the day a simultaneous boot stops costing instances, and that flip is the signal to
close this entry rather than a test to repair.

=== What the harness does about it, and why that is not a fix

`HuntWorld` resolves the storage identifier once, on one node, before releasing the others from the boot barrier, and
`HuntNode` records a failed start and gives the node one more try. Neither touches framework code. The first is there
because without it the segment-creation race is unreachable -- every multi-node scenario would stop at this one
defect instead of testing what it was written for. The second is what a supervised deployment does with a process
that died on startup. Both are stated here so that nobody reads a green cluster run as evidence that F-9 is closed:
the only thing that closes it is the expected-gap test above going red.

== F-10 -- The claim protocol states no tolerance for clock skew, and tolerates none

*Severity:* Medium +
*Status:* Open. *Reproduced by test*, quantified, and *model-checked* -- the model overturned one of the three measured
bounds, and the candidate fix below is the corrected one. +
*Claims:* C19, C20, C22, M3 +
*Backend verdict vector:* `hsqldb-tokens:AFFECTED`, `in-memory:N/A (the in-heap token store has no expiry to compare)`

=== What is wrong

Whether a claim may be stolen is the inequality `timestamp + claimTimeout < now`. The timestamp is written by whichever
node last touched the row; `now` is read by whichever node is asking. The two are different machines in every deployment
the token store exists for, and nothing in the framework states how far apart their clocks may be. Searching `skew`,
`clockSkew` and `clock skew` across the token-store packages and the streaming-processor reference page returns nothing.

=== Evidence

* `messaging/src/main/java/org/axonframework/messaging/eventhandling/processing/streaming/token/store/jdbc/JdbcTokenEntry.java:202-207` -- `mayClaim` and `expired`, the whole of the arbitration
* `messaging/src/main/java/org/axonframework/messaging/eventhandling/processing/streaming/token/store/jpa/TokenEntry.java:159-161` -- the same comparison on the JPA store
* `docs/reference-guide/modules/events/pages/event-processors/streaming.adoc` -- documents the ten-second claim timeout and says nothing about the clocks it is measured with

=== What the reproduction measured

The scenario `at_most_one_segment_owner_with_skew_none` and its two skewed siblings run four nodes over sixteen segments
against the claim-capable store, with one node's view of that store configured to treat a claim as expired earlier than
the run's claim timeout says. That is not an approximation: a node whose clock reads `delta` ahead evaluates
`timestamp + claimTimeout < now + delta`, which is the same inequality as `timestamp + (claimTimeout - delta) < now`, so
shortening one node's claim timeout reproduces its decisions exactly. What it does not model is the timestamp that node
*writes*, which a real skewed clock would also shift; that is unreachable, because the framework stamps a claim from a
process-global clock every node in one virtual machine shares (finding F-4).

Measured against a two-second claim timeout and a four-hundred-millisecond extension threshold:

[source]
----
skew 0ms     widest overlap  none          9 claim handovers across 3 seeds, all orderly
skew 1000ms  widest overlap  964 - 992ms   inside the declared tolerance of 1000ms
skew 4000ms  widest overlap  478 - 1692ms  16 to 32 AtMostOneSegmentOwner violations
----

Three things follow, and the third is the one worth acting on.

*The overlap is bounded by the skew.* A node reading `delta` ahead can take a claim at most `delta` before it would have
lapsed anyway, so the overlap can never exceed `delta` -- which the middle arm asserts and the oracle judges.

*It is also bounded by one claim timeout.* At four seconds of skew against a two-second timeout the overlap saturates:
the measured worst case is 1692ms, not 4000ms. The loser's own claim still lapses one timeout after its last refresh
however far ahead the thief's clock reads, because only the comparison is skewed and not the timestamp.

*A skew smaller than the margin between the claim timeout and the owner's refresh rate is usually invisible, and that is
not the same as tolerated.* An owner refreshes its row with every batch it stores and, when idle, on every extension
threshold. With a two-second timeout and a four-hundred-millisecond threshold, a row is rarely more than four hundred
milliseconds old, so a clock a second ahead usually finds nothing to take -- and occasionally does, which is why the
middle arm's measured overlap sits just under its bound rather than at zero. So the margin between the claim timeout and
the refresh rate looks like a tolerated skew, and it is not one; see the model check below.

=== What the model check settled, and what it overturned

A design model of the same inequality, `formal/tla/TokenClaim.tla`, was checked with TLC over two nodes and one segment
at a claim timeout of five ticks against a refresh interval of one -- the same 5:1 ratio as the arms above. It confirms
two of the three statements and overturns the third.

The two bounds hold. With the forgiven overlap set equal to the skew, `MCClaim_skew_bounded_by_skew.cfg` holds across
1506 states, so the overlap really cannot exceed the skew. At twice the claim timeout,
`MCClaim_skew_double.cfg` holds with the forgiven overlap set to one claim timeout (4838 states) while
`MCClaim_skew_double_tight.cfg` is violated with it set one tick lower, so the overlap really does reach exactly one
claim timeout and never more.

*The third statement is false as an unconditional claim.* `MCClaim_skew_below_margin.cfg` runs a skew of two ticks
against a margin of four -- comfortably inside the supposedly invisible region -- and TLC finds a violation in twelve
steps: the owner claims the row at tick 0, four ticks pass without it refreshing, and the skewed node then reads tick 6,
finds `0 + 5 < 6`, and takes the row while the owner's own interval still runs to tick 5. Its sibling
`MCClaim_skew_below_margin_fixed.cfg`, which forbids the clock to advance past the point at which a running owner owes a
refresh, holds across 308 states.

So `claimTimeout - refreshInterval` is a bound on a *punctual* owner, not on the protocol. Nothing makes an owner
punctual: refreshing is work a scheduled thread does when it is scheduled, and one missed window is enough. The
measurement's own hint -- overlaps of 964 to 992 ms at a skew of 1000 ms against a margin of 1600 ms -- is exactly this
mechanism, and until the model check it was written off as the arm occasionally getting lucky.

=== Why it matters

Two nodes holding one segment for up to a claim timeout means two nodes processing the same events for that long. The
framework's own answer to a stolen claim is that the loser's token write fails and its batch rolls back, so effects
already applied are repeated -- which is the documented at-least-once behaviour and is fine for an idempotent handler and
not fine for anything else. What is missing is any statement of how much clock disagreement it takes to get there.

=== Candidate fix

Document the bound that is actually a bound. *The claim protocol tolerates no clock skew at all*: any skew can produce
overlapping ownership, and the overlap is at most the lesser of the skew and one claim timeout. An operator sizing
`claimTimeout` and `claimExtensionThreshold` needs that sentence to size them safely.

What must *not* be documented is the margin between the claim timeout and the refresh rate. It was the obvious candidate
before the model check -- a skew below it is usually invisible -- and the model check shows it holds only while the owner
refreshes on schedule, which nothing guarantees. Publishing it would publish a tolerance the framework does not enforce,
which is worse than publishing none.

Separately, the expiry comparison could carry an explicit tolerance, so that the bound becomes a setting rather than an
emergent property of two other settings and of how punctually a background thread gets scheduled.

=== Reproduce

[source,bash]
----
./mvnw -Phunt -pl simulation -am test -Dtest=SegmentOwnershipUnderSkewTest \
    -Dsurefire.failIfNoSpecifiedTests=false
----

The double-timeout arm asserts that ownership *is* broken, because the suite records the framework's current behaviour
and never patches it. It flips red the day the claim protocol gains a stated tolerance wide enough to cover it, and that
flip is the signal to revisit this entry.

The model check, which needs no cluster and takes about a second per configuration:

[source,bash]
----
java -XX:+UseParallelGC -cp formal/tla/tools/tla2tools.jar tlc2.TLC \
    -workers auto -metadir formal/tla/states \
    -config formal/tla/MCClaim_skew_below_margin.cfg formal/tla/TokenClaim.tla
----

`formal/tla/README.md` has the jar fetch and the other five configurations.

== F-11 -- A merge rewinds to the lower of the two tokens, and nothing says so

*Severity:* Medium +
*Status:* Open. *Measured once; licensed since, and asserted nowhere.* +
*Reproduction state:* *(b) probed.* `SplitAndMergeUnderLoadTest` runs the split/merge storm and asserts that splits and
merges landed and that `result.violations()` is **empty** -- because `DeliveryChecker` deliberately licenses the
redelivery a merge causes. So the arm passes *because* this finding is true, and it would go on passing if it stopped
being true. The redelivery reaches stdout only through the checker's free-text measurement
(`Repeated deliveries, by how many times an event arrived: ...`), which folds claim handovers, crash recovery and replay
into the same counter and is not merge-specific. *Reaching (a) is not cheap:* a merge record carries `segment` and
`carriedOut` but no token positions, so pinning "the surviving segment took the lower token" needs a new counting helper,
and removing the licence instead would turn the arm red rather than pin the finding. +
*Claims:* C24, C25, C38, M10

=== What is wrong

`MergeTask` merges two segments by giving the surviving one the lower of the two halves' tracking tokens. Every event the
further-ahead half had already handled therefore sits above the merged segment's position and is delivered again. The
behaviour is deliberate -- taking the higher token would skip events the lower half had not reached -- and it is the right
choice. What is missing is any statement of it: `Segment`'s Javadoc describes the mask algebra, `MergeTask`'s describes
when a merge succeeds, and neither mentions that a merge redelivers.

=== Evidence

* `messaging/src/main/java/org/axonframework/messaging/eventhandling/processing/streaming/pooled/MergeTask.java:126-133` -- both tokens fetched, then merged lower-first
* `messaging/src/main/java/org/axonframework/messaging/eventhandling/processing/streaming/segmenting/Segment.java:141-142` -- the documented merge precondition, which is about masks and not about positions

=== What the reproduction observed

`split_merge_no_loss_no_dup_under_load` splits and merges a segment repeatedly while the workload writes, seven times per
seed inside a four-second window. Before the delivery oracle was taught about it, the arm reported repeated deliveries
outside every claim-transition and node-recovery window -- events like `writer-7-327-w` arriving twice with nothing in the
history that licensed it. The mechanism is the merge, and the events repeated are exactly the ones between the two halves'
tokens.

Nothing is lost: the union of deliveries across every segment epoch still covers every committed event, and the ledger's
conservation law holds once the projection is idempotent -- which the framework's own documentation requires of a handler
in a split-resource deployment.

=== Why it matters

A projection that is not idempotent double-applies on every merge. The framework already tells users to make handlers
idempotent for the split-resource deployment, so the requirement is not new; what is new is that a merge triggers it
even on a cluster that never lost a claim and never crashed. An operator who split segments to add throughput and then
merged them back has redelivered part of the stream, and nothing warned them.

=== Candidate fix

Documentation. State on `StreamingEventProcessor.mergeSegment` and on the reference page that a merge resumes from the
lower of the two segments' positions, so events already handled by the further-ahead half are handled again, and that
handlers must therefore be idempotent across a merge regardless of the token store's deployment.

=== Reproduce

[source,bash]
----
./mvnw -Phunt -pl simulation -am test -Dtest=SplitAndMergeUnderLoadTest \
    -Dsurefire.failIfNoSpecifiedTests=false
----

`DeliveryChecker` now opens a licence window at every recorded split and merge, so the arm passes and reports the
repeats as a distribution rather than as a violation. Removing that licence turns the finding back into a red run, which
is how it was found.

== F-12 -- Resetting tokens demands a converter even when there is nothing to convert

*Severity:* Low +
*Status:* Open. *Confirmed by reading. No test exercises it: the harness supplies the converter before the defect can
fire.* +
*Reproduction state:* *(c) inspected.* The named arm `ReplayAfterResetTest` exercises the reset and **passes**, because
`HuntWorld` hands the processing context a converter precisely so that the run can get past this. No assertion and no
print in the suite names a converter, a `GeneralConverter` or an `UnsupportedOperationException`. Reaching (a) would mean
a scenario wired over an empty application context, whose only purpose is to fail -- the inspection below is worth more
than that for a Low-severity ergonomics gap. +
*Claims:* C26, C28

=== What is wrong

`PooledStreamingEventProcessor.resetTokens` converts the reset context to a byte array through a `GeneralConverter` it
resolves from the processing context, and it does so unconditionally -- including for `resetTokens()`, whose reset
context is `null`. A processor built without a configuration gets the framework's own default unit-of-work factory, whose
application context provides no components at all, so the resolution throws before the reset does anything.

=== Evidence

* `messaging/src/main/java/org/axonframework/messaging/eventhandling/processing/streaming/pooled/PooledStreamingEventProcessor.java:346-352` -- `convertedResetContext(resetContext, processingContext.component(GeneralConverter.class))`, called for every segment
* `messaging/src/main/java/org/axonframework/messaging/eventhandling/configuration/EventProcessorConfiguration.java:65` -- the default factory, built over `EmptyApplicationContext`
* `messaging/src/main/java/org/axonframework/messaging/core/EmptyApplicationContext.java:44-49` -- which throws `UnsupportedOperationException` for every request

=== Why it matters

The failure names neither resets nor converters: it is an `UnsupportedOperationException` reading "EmptyApplicationContext
does not provide any components", raised from inside the reset's unit of work. A plain-Java application that assembles a
processor by hand -- which the framework supports and this suite does -- can start it, run it, split it and merge it, and
then discover that it cannot be reset, for a reason that has nothing to do with what it asked for.

=== Candidate fix

Skip the conversion when there is nothing to convert: a `null` reset context needs no converter, and
`convertedResetContext` already returns an empty array for it. One guard removes the requirement for every caller of the
no-argument reset. Separately, resolving the converter once before the per-segment loop rather than once per segment
would make the failure, when it is genuine, arrive once.

=== Reproduce

*By inspection, in seconds and with no build.* The three sites are the unconditional resolution, the default context it is
resolved from, and what that context does when asked:

[source,bash]
----
grep -n 'convertedResetContext' \
  messaging/src/main/java/org/axonframework/messaging/eventhandling/processing/streaming/pooled/PooledStreamingEventProcessor.java
grep -n 'does not provide any components' \
  messaging/src/main/java/org/axonframework/messaging/core/EmptyApplicationContext.java
----

The first prints the call site at `:351` and the helper at `:359-361`, whose own body shows that a `null` reset context
needs no converter at all -- `convertedResetContext == null ? new byte[0] : ...`. The second prints the message the caller
actually gets, at `:46`. Nothing between them mentions a reset.

*There is no automated reproduction, and the arm named here does not provide one.* This exits 0:

[source,bash]
----
./mvnw -Phunt -pl simulation -am test -Dtest=ReplayAfterResetTest \
    -Dsurefire.failIfNoSpecifiedTests=false
----

The harness supplies the converter through the processor's unit-of-work factory -- see the Javadoc on
`HuntWorld.HARNESS_COMPONENTS`, which records why -- so the arm exercises the reset and never reaches this defect.
Removing that one line reproduces it.

== F-13 -- A node stalled in event handling keeps every claim it holds

*Severity:* Low, and not a defect +
*Status:* Measured. Recorded as a harness constraint. +
*Claims:* C20, C21, M1

=== What was expected, and what happens

The classic lease failure is a process that stalls -- a garbage-collection pause, a frozen virtual machine -- long enough
for its claims to expire, and then wakes up mid-batch into a cluster that has reassigned its work. A suite that only
kills processes never visits it, which is why this one has a stall seam.

The stall this suite can reach is inside the event handler, and it does not produce that state. Extending a claim is the
coordinator's work, on a thread of its own, and the coordinator keeps running while the handler is frozen: it calls
`WorkPackage.extendClaimIfThresholdIsMet()` for every package it holds on every coordination cycle. The frozen node's
rows therefore stay fresh for as long as the freeze lasts, and nothing expires.

=== Evidence

* `messaging/src/main/java/org/axonframework/messaging/eventhandling/processing/streaming/pooled/Coordinator.java:814` -- the coordinator extending its packages' claims
* `messaging/src/main/java/org/axonframework/messaging/eventhandling/processing/streaming/pooled/WorkPackage.java:470-479` -- the extension, in its own unit of work, independent of any batch

=== Consequences

For the framework this is correct and arguably better than the alternative: the process is alive, so its claims are
alive. It is worth documenting, because the reference page's account of claim stealing reads as though a slow owner loses
its segments, and a slow *handler* does not.

For this suite it is a constraint on what the pause seam can test. Reaching the stalled-owner state needs the whole
process stalled -- which, in one virtual machine, means stalling the node's access to the token store as well as its
handling. The ownership scenario therefore uses a node crash to lapse a claim, and carries no stall window: a fault that
cannot take a segment away, and that lands only while the node is still handling events, is noise rather than evidence.

=== Reproduce

By inspection:

[source,bash]
----
grep -n 'extendClaimIfThresholdIsMet' messaging/src/main/java/org/axonframework/messaging/eventhandling/processing/streaming/pooled/Coordinator.java
----

== F-14 -- An unconditional append is rejected as conflicting on the aggregate-based store

*Severity:* High +
*Status:* Open. *Reproduced by test*, and *model-checked* (violated/fixed pair). +
*Claims:* C2, C10 +
*Backend vector:* `in-memory:PASS hsqldb-tokens:PASS postgres-jpa:FAIL postgres-jpa-split-tokens:FAIL`

=== What is wrong

`AppendCondition.none()` is the framework's way of appending without a consistency condition, and the documentation is
explicit about what it means: conflict detection is disabled entirely, its marker is `INFINITY`, and the conflict check
short-circuits. On the aggregate-based JPA event store it does not mean that. The engine derives each event's aggregate
sequence number from the append condition's marker, and an `INFINITY` marker carries no position for any aggregate, so
the sequencer starts that aggregate at sequence `0`. For an aggregate that already holds events, inserting sequence `0`
again violates the unique constraint the engine relies on as its conflict check, and the failure is translated into
`AppendEventsTransactionRejectedException` -- the framework's own "you conflicted, source again and retry" answer, for an
append that asked for no consistency at all. The retry then fails identically, for ever.

The same line treats `ORIGIN` and `INFINITY` as the same thing, so "append with no condition" and "append only if this
aggregate is empty" are indistinguishable on this engine.

=== Evidence

* `eventsourcing/src/main/java/org/axonframework/eventsourcing/eventstore/AggregateBasedConsistencyMarker.java:72-74` --
  `else if (consistencyMarker == ORIGIN || consistencyMarker == INFINITY) { return new
  AggregateBasedConsistencyMarker(Map.of()); }`, an empty map of aggregate positions for both sentinels
* `eventsourcing/src/main/java/org/axonframework/eventsourcing/eventstore/jpa/AggregateBasedJpaEventStorageEngine.java:248-256`
  -- the entry's sequence number comes from `sequencer.incrementAndGetSequenceOf(aggregateIdentifier)`, which for an
  empty marker starts at `0`
* `eventsourcing/src/main/java/org/axonframework/eventsourcing/eventstore/AggregateBasedEventStorageEngineUtils.java:104-124`
  -- the duplicate-key violation is translated into `AppendEventsTransactionRejectedException`
* `eventsourcing/src/main/java/org/axonframework/eventsourcing/eventstore/NoAppendCondition.java:42-44` -- the
  documented `INFINITY` marker of `AppendCondition.none()`

=== What the reproduction observed

The ledger workload's `rebalance` command appends without sourcing, which is exactly how the framework produces an
unconditional append. Run unchanged against both PostgreSQL arms, `AppendOutcomeChecker` raised 231 and 259
`UnconditionalAppendNeverRejected` violations respectively, all of the form "an append anchored at the end of the stream
was rejected as conflicting". The same scenario, the same workload and the same oracle raise nothing on either store that
speaks the Dynamic Consistency Boundary protocol, which is what makes the vector an attribution rather than an argument.

=== Why it matters

An unconditional append is the documented way to write an event that takes part in nobody's consistency boundary, and
users reach for it precisely where they do not want a conflict check. On this engine it works for the first event of an
aggregate and never again, and the error it raises is the one that tells the caller the write is safe to retry after
re-sourcing. A projection-driven or reference-data write path built on `AppendCondition.none()` therefore appears to
work on an in-memory or Dynamic Consistency Boundary store and stops working on the JPA engine, with an exception that
names a conflict that does not exist.

=== Candidate fix

An `INFINITY` marker must mean "append at the end", not "append at sequence zero". Resolve each tagged aggregate's
current highest sequence number before assigning, for an unconditional append only, and let the append take the next
sequence for each. `ORIGIN` must keep its current behaviour -- starting at zero is precisely what "the aggregate must be
empty" means -- so the two sentinels have to stop sharing a branch. Alternatively, if an aggregate store genuinely cannot
honour an unconditional append, the engine should reject it with an exception that says so rather than with a conflict
its caller will retry.

=== Reproduce

[source,bash]
----
./mvnw -Phunt -pl simulation -am test -Dhunt.excludedGroups=fuzz -Dtest=BackendDifferentialTest \
    -Dsurefire.failIfNoSpecifiedTests=false
----

The run prints one `VECTOR` line per scenario. The finding is the `postgres-jpa:FAIL` entries against `in-memory:PASS`.

=== The same defect at the design level

`formal/tla/DcbAppend.tla` models the append protocol with one flag, `INFINITY_IS_ORIGIN`, that turns the collapse above
on and off. Turning it on reproduces the finding as a TLC counterexample five states long: a writer appends one event
under a boundary, a second writer takes `AppendCondition.none()`, offers its own batch, and is rejected -- because the
collapsed marker made the engine scan a store that has an event in it. `MCAppend_unconditional.cfg` reports
`Invariant UnconditionalAppendNeverRejected is violated`; `MCAppend_unconditional_fixed.cfg`, the same run with the
collapse off, holds across its whole 2784-state space. `MCAppend_conformance.cfg` shows the same divergence through the
primary oracle's invariant, `AppendConformsToDcbModel`.

The model also settles something no suite run can. `MCAppend_illegalcommit.cfg` runs the divergent engine and checks
`AppendIsLegalIffNoMatchInScanRange` and `CommitMarkerNeverRegresses`, and both hold across all 5532 states: *the
collapse is over-rejection only*. It never accepts an append the protocol forbids and never lets a reported marker go
backwards. A run can only report the divergences it happened to produce; the model checks for the dangerous direction and
does not find it, at those bounds.

[source,bash]
----
java -XX:+UseParallelGC -cp formal/tla/tools/tla2tools.jar tlc2.TLC \
    -workers auto -metadir formal/tla/states \
    -config formal/tla/MCAppend_unconditional.cfg formal/tla/DcbAppend.tla
----

== F-15 -- Sourcing twice in one processing context throws on the aggregate-based store

*Severity:* High +
*Status:* Open. *Reproduced by test.* +
*Claims:* C5, C6, C7 +
*Backend vector:* `in-memory:PASS postgres-jpa:FAIL`

=== What is wrong

`DefaultEventStoreTransaction` combines consistency markers when a processing context sources more than once: the first
sourcing's marker is kept as the append position, and every later one is folded in with `lowerBound`, because the lower of
the two is the safe anchor. `AggregateBasedConsistencyMarker.doLowerBound` throws
`UnsupportedOperationException("Not implemented yet")`, so on the aggregate-based JPA event store the second sourcing in a
processing context fails outright.

This is not an exotic path. A command handler that loads two entities, or one that loads the same entity twice, or a
multi-entity handling component, sources twice by construction, and none of that is unusual or discouraged.

=== Evidence

* `eventsourcing/src/main/java/org/axonframework/eventsourcing/eventstore/AggregateBasedConsistencyMarker.java:79-82` --
  `public AggregateBasedConsistencyMarker doLowerBound(AggregateBasedConsistencyMarker other) { throw new
  UnsupportedOperationException("Not implemented yet"); }`
* `eventsourcing/src/main/java/org/axonframework/eventsourcing/eventstore/DefaultEventStoreTransaction.java:144-153` --
  `updateAppendPosition`, which calls `current.lowerBound(marker)` for every sourcing after the first
* `eventsourcing/src/main/java/org/axonframework/eventsourcing/eventstore/DefaultEventStoreTransaction.java:129` -- the
  `onComplete` that calls it
* the same class's `doUpperBound` at `:84-92` is implemented, so the missing half is not a design decision about
  aggregate markers in general

=== What the reproduction observed

The existing integration-test suite, unchanged, run against PostgreSQL through the run-time backend selection. Of the 65
cases in the suite, three failed with this exception and no other cause:

* `MultiEntityCommandHandlingComponentSuiteIT.canCombineModelsInAnnotatedCommandHandlerViaStateManagerParameter`
* `MultiEntityCommandHandlingComponentSuiteIT.canCombineStatesInLambdaCommandHandlerViaStateManagerParameter`
* `CommandSequencingSuiteIT.concurrentEnrollmentsToSameCourseDoNotCauseOptimisticLockingFailures`, which reports it as
  `Expected no exceptions during concurrent enrollments, but got: java.lang.UnsupportedOperationException`

All three combine two entities in one command, which is what makes them source twice, and all three pass on the in-memory
store, whose marker implements `lowerBound`. The two multi-entity cases are the plainest statement of the blast radius:
combining two models in one handler is a documented, first-class thing to do, and it cannot be done on this store.

=== Why it matters

The exception names neither markers nor sourcing to the person who hits it, and it arrives from inside a command that did
nothing unusual. An application built and tested against the in-memory store will hit it the first time it runs against
the JPA engine, on any handler that sources twice -- which is most non-trivial ones.

=== Candidate fix

Implement it as the mirror of `doUpperBound`: take the lower sequence number per aggregate identifier, and the union of
the identifiers, so that an aggregate known to only one of the two markers keeps its position. If there is a reason the
lower bound of two aggregate markers is genuinely undefined, the throw should say that instead of "Not implemented yet",
and `DefaultEventStoreTransaction` should not reach it.

=== Reproduce

[source,bash]
----
./mvnw -Pintegration-test -pl integrationtests verify -Djacoco.skip=true \
    -Dtest=NoSuchUnitTest -Dsurefire.failIfNoSpecifiedTests=false -Dhunt.backend=postgres-jpa
----

The same command without `-Dhunt.backend` runs the identical classes against the in-memory store and is green, which is
the vector.

== F-16 -- An event skipped for ever because its own timestamp aged past the gap timeout

*Severity:* High +
*Status:* Open. Measured on PostgreSQL, on both configuration paths. *The loss assertion is conditional on the skip having
happened, so it is not a pin.* +
*Reproduction state:* *(b) probed.* `StoreInfrastructureFailureTest$HoldingCommitsOpenPastTheStoresGapTimeout` prints
`missing=<n> lossViolations=<n>` on both arms every run, and asserts the loss is judged rather than excused **inside**
`if (stalled(result) && missing > 0)`. Close the gap and `missing` becomes zero, the branch never runs, and the arm goes
green without anything having been checked. What it asserts unconditionally is that the fault landed and that the drain
reported its shape. *Reaching (a) is one line* -- `assertThat(missing).isPositive()` on the already-computed local -- but
it asserts that a race fires on every run, and the rate has never been measured. An earlier phase declined it on exactly
those grounds and this one keeps that call: **measure the rate across runs first, then pin it.** That is the follow-up. +
*Backend vector:* `in-memory: n/a` (no gaps to record -- an in-heap map has no index taken before a commit),
`hsqldb-tokens: n/a` (same event store), `postgres-jpa-chaos: FAIL`, `postgres-jpa-chaos-spring-defaults: FAIL`,
`postgres-jpa: intermittent`, `postgres-jpa-split-tokens: FAIL`.

The two `-chaos` arms hold a transaction open on purpose, so they reproduce it deliberately. The two arms that install no
fault at all reproduce it by accident, which is the more alarming half: `postgres-jpa-split-tokens` lost 2 and 4 committed
events on its two seeds of a clean engine, and `postgres-jpa` lost 0 on both seeds of one run and 2 on a seed of another.
Nothing forces the interval on those arms -- ordinary contention over a database is enough.

=== What is wrong

`AggregateBasedJpaEventStorageEngine` takes its `globalIndex` from a database sequence *before* the transaction commits,
so a reader routinely sees index `n+1` while `n` is a hole that will later fill. Recording that hole as a gap is what
makes the reader come back for it. Whether it records one is decided here:

[source,java]
----
// eventsourcing/src/main/java/org/axonframework/eventsourcing/eventstore/jpa/AggregateBasedJpaEventStorageEngine.java:427
boolean allowGaps = timestamp.isAfter(gapTimeoutThreshold);
return token == null
        ? GapAwareTrackingToken.newInstance(globalIndex, calculateGaps(globalIndex, allowGaps))
        : token.advanceTo(globalIndex, allowGaps ? maxGapOffset : 0);
----

`timestamp` is the **message's own** timestamp and `gapTimeoutThreshold` is `now - gapTimeout`
(`GapAwareTrackingTokenOperations.gapTimeoutThreshold()`). So an event that has aged past the threshold by the time a
reader first sees it advances the token to its index with a gap allowance of **zero**, and every lower index still
uncommitted at that instant is never recorded as a gap at all. When those transactions commit, the reader's token is
already past them and holds nothing that would bring it back.

The decisive point is that the ageing is not the *reader's* lateness. It is the interval between the event being created
in the handler and its transaction committing, and any of the ordinary reasons for that interval to be long -- a slow
store, a contended lock, a large transaction, a paused process -- is enough.

=== Evidence

Measured on the breakable PostgreSQL arm, with every commit held open between the rows being written and the transaction
committing, for twice the arm's gap timeout. The hold is what makes it reliable, not what makes it possible:

----
gap-core   readableEvents=88  deliveredEvents=85  stalled=true
  [NoCommittedEventGoesUndelivered] 3 committed event(s) never reached a consumer, for example
  [writer-3-4-w, writer-6-5-w, writer-6-10-w]; the read side had stopped accepting deliveries altogether,
  so nothing was in flight
gap-spring readableEvents=86  deliveredEvents=83  stalled=true
  [NoCommittedEventGoesUndelivered] 3 committed event(s) never reached a consumer, for example
  [writer-5-0-w, writer-5-0-d, writer-6-7-w]; the read side had stopped accepting deliveries altogether,
  so nothing was in flight
----

The events are in the store: the count they are missing from is a direct SQL scan of `aggregateevententry`, taken on a
connection of its own. The read side had stopped moving entirely for longer than the run's stall window, so nothing was
in flight, which is what makes this loss rather than lateness.

And on the arms that install no fault, at the framework's own 60-second gap timeout, on a clean engine:

----
MEASURED postgres-jpa-split-tokens seed 1 undelivered=2 decided=true caughtUp=false
MEASURED postgres-jpa-split-tokens seed 2 undelivered=4 decided=true caughtUp=false
MEASURED postgres-jpa              seed 1 undelivered=0 decided=false caughtUp=true
MEASURED postgres-jpa              seed 2 undelivered=0 decided=false caughtUp=true
----

Two things follow that the deliberate arms cannot establish on their own. The loss does not need a fault, and the events it
takes sit near the *start* of the store rather than the end -- in one history at store positions 47 and 50, committed at
2620ms, with the reader having delivered up to position 592 by 5592ms and then nothing for the following sixty seconds. It
is not a tail that ran out of time.

**What is not established is the mechanism at the default timeout.** The `allowGaps` comparison explains the compressed
arms exactly: a hold of twice the gap timeout guarantees the ageing. At sixty seconds it does not obviously apply to an
event committed 2.6 seconds into a run, and no attempt was made here to instrument the reader's token to find out which of
the gap machinery's several paths took it. The observation is reproducible and the mechanism at that timeout is open.

=== Both configuration paths, because they disagree

The two arms differ only in the gap settings, and the settings differ because the two configuration paths default them to
each other's values (finding F-1): the framework's own configuration record defaults `gapTimeout` to 60000ms and
`maxGapOffset` to 10000, and Spring Boot's autoconfigure defaults them to 10000ms and 60000. Both arms skip. So this is
not a consequence of one path's numbers; it is the mechanism, and the numbers only decide how long a transaction has to
be open before it bites -- ten seconds on a Spring Boot deployment and a minute on a plain one.

=== Why it matters

The loss is silent in every direction. The append succeeded and the caller was told so. The store holds the event and any
scan finds it. No error is logged, no token is refused, no exception reaches anybody. The only observable is that a
projection is permanently short of an event, discovered whenever somebody reconciles a read model against the store --
which for an event-sourced system is the one comparison that is supposed to be redundant.

=== How it was found

By holding a transaction open between the rows being written and the transaction committing, which is the only interval in
which the hole exists. Canary C7 -- forcing `allowGaps = false` in that same comparison -- turns the shared-resource arm
from `undelivered=0` on both seeds to `undelivered=3` and `undelivered=6`, which is the mutation reproducing a stronger
form of the same defect and is why C7 has stopped being useful as a *planted* defect. It took two attempts: the first delayed
`EventStorageEngine.AppendTransaction.commit()` and changed nothing at all, because on this engine that call does no work
and races the database transaction rather than preceding it (finding F-17).

It also needed the loss oracle to be able to decide. `DeliveryChecker` declined to call an event lost on a run whose read
side had not caught up -- correctly, because an interrupted run has lost nothing -- and a store that skips an index for
ever produces exactly that observation. The run now reports whether the read side had *stopped*, and loss is decided on a
stalled read side exactly as it is on one that caught up. That is the same guard that made canary C7 escape, closed.

=== Candidate fix

The comparison is between an event's own timestamp and the reader's clock, and those two are not commensurable: one is set
when the event is created and the other is read when the event is first seen, with an unbounded interval between them
that the framework does not control. Two candidates, neither free:

* compare against something the store assigns at commit time rather than against the message timestamp, so that the
  threshold measures how long the *index* has been outstanding rather than how old the payload is;
* or treat a hole below an aged event as a gap regardless, and rely on `gapCleaningThreshold` and `maxGapOffset` to bound
  the cost, accepting the read amplification the current comparison exists to avoid.

Whichever is chosen, the reference documentation for `gapTimeout` describes it as a performance setting for cleaning old
gaps and does not say that exceeding it can drop an event. That sentence is a fix in itself.

=== Reproduce

[source,bash]
----
./mvnw -Phunt -pl simulation -o test -Dhunt.excludedGroups=fuzz \
    -Dtest='StoreInfrastructureFailureTest$HoldingCommitsOpenPastTheStoresGapTimeout' \
    -Dsurefire.failIfNoSpecifiedTests=false
----

Needs Docker. The arm prints its own verdict vector and the count of events the store held that never reached a consumer.

== F-17 -- The database transaction's commit races the append transaction's commit

*Severity:* Medium +
*Status:* Open. Measured once, under a fault that has since been moved. *The measurement cannot be reproduced from the
committed tree; the ordering claim can be read off the source in seconds.* +
*Reproduction state:* *(c) inspected.* Nothing in the suite compares a delivery timestamp against a `commit()` entry, and
the arm named below no longer produces the observation: `LateCommitFault` now delays `StoreHook.afterAppend`, which is
strictly before the transaction, so a current run shows the correct order. Reverting the fault to delay `onCommit`
reproduces it. The inspection under Reproduce shows the part that does not need a run: both actions are registered in the
same lifecycle phase, and the framework orders same-phase actions not at all. +
*Backend vector:* `in-memory: n/a` (no database transaction -- the engine's own `commit()` is the commit),
`postgres-jpa: measured`, `postgres-jpa-chaos: measured`.

=== What is wrong

On a store whose transaction lives on the processing context, two separate actions land in the framework's `COMMIT`
phase, and the framework states plainly that it will not order them:

* the transaction manager registers its commit during `PRE_INVOCATION` --
  `TransactionManager.attachToProcessingLifecycle` does `processingLifecycle.runOnPreInvocation(pc -> { Transaction t =
  startTransaction(); pc.runOnCommit(p -> t.commit()); ... })`;
* the event store registers the append transaction's commit during `PREPARE_COMMIT` --
  `DefaultEventStoreTransaction.attachAppendEventsStep()` does `processingContext.onCommit(c -> tx.commit()...)` from
  inside its own prepare-commit action.

Both are `DefaultPhases.COMMIT`, and `ProcessingLifecycle`'s own Javadoc says that "actions registered in a `Phase` with
the same order may be executed in parallel"; `UnitOfWork` combines them with `CompletableFuture::allOf`.

So on such a store the events become durable and readable at a moment that has no defined relationship to
`AppendTransaction.commit()`. It may be before, during or after.

=== Evidence

From one run's history, for the first event delivered on the breakable PostgreSQL arm, with a two-second delay installed
after the rows were written:

----
event writer-3-0-w delivered at ms 615
  commit INVOKE ms 2492
  append OK   ms 2492
----

The event was handed to a consumer 1877ms before the append transaction's `commit()` was even entered. The engine's own
`commit()` is a documented no-op on this store -- "Transaction is in control of the entity manager executor, so do no
work here" -- so nothing was waiting for it.

=== Why it matters

Three consequences, and the third is the one that bites.

`AppendTransaction.commit()` cannot fail an append on such a store. By the time it runs, the events may already be
readable, so a failure there is a failure after the point of no return rather than a refusal.

`AppendTransaction.rollback()` cannot undo one either. The framework registers it through `onError` for any phase, and it
is already documented that a failure after the commit rolls back a batch that has been published (finding F-8) -- what is
new here is that the same is true for a failure *during* the commit phase, because the transaction may have gone first.

And the interface says none of this. `EventStorageEngine.AppendTransaction` reads as a two-phase protocol in which
`commit()` is the moment of durability, and for the in-heap engine it is; for a transaction-managed engine it is not, and
an implementer has no way to know which contract they are writing to. Every harness or interceptor that treats
`commit()` as the visibility point is wrong on half the stores -- which is exactly the bug this suite had, and is how
this was found.

=== How it was found

A fault was written to hold a transaction open by delaying `AppendTransaction.commit()`, on the reasonable assumption
that the transaction commits after it. The arm produced 154 `NoVisibilityBeforeCommit` violations and no skip at all,
which is the opposite of what it was built to find. Reading the delivery and commit timestamps out of the history showed
the delay was entirely after durability.

=== Candidate fix

Either give the two actions an order -- the storage engine's commit registered in a phase strictly before the transaction
manager's, so that `commit()` really is the last thing that can refuse an append -- or say in
`EventStorageEngine.AppendTransaction`'s Javadoc that on a store whose transaction is external, `commit()` is not the
durability point and must not be treated as one. The second is a documentation change and closes the trap for
implementers; the first closes it for callers as well.

=== Reproduce

[source,bash]
----
./mvnw -Phunt -pl simulation -o test -Dhunt.excludedGroups=fuzz \
    -Dtest='StoreInfrastructureFailureTest$HoldingCommitsOpenPastTheStoresGapTimeout' \
    -Dsurefire.failIfNoSpecifiedTests=false
----

Then read any history under `simulation/target/hunt-histories/gap-core-defaults/` and compare a `deliver` record's
`logicalTs` against the `commit` record carrying the same event identifier. *The fault now delays
`StoreHook.afterAppend`, which is strictly before the transaction, so a current run shows the correct order* -- reverting
`LateCommitFault` to delay `onCommit` reproduces the measurement above, and until someone does, the command above does not
demonstrate this finding.

*The part that needs no run.* The engine's `commit()` does nothing on this store, so nothing about durability is waiting
for it:

[source,bash]
----
grep -n 'Transaction is in control of the entity manager executor' \
  eventsourcing/src/main/java/org/axonframework/eventsourcing/eventstore/jpa/AggregateBasedJpaEventStorageEngine.java
----

Two hits, at `:216` and `:222` -- the bodies of `commit()` and of the transaction's other action. A store whose
`commit()` is documented as doing no work cannot be the point at which its events become durable, and nothing on
`EventStorageEngine.AppendTransaction.commit()` says it need not be.

== F-18 -- The released connector misses an abstract storage-engine method

*Severity:* Medium -- a released client library cannot be loaded against this reactor. +
*Status:* Open in the framework and its connector. **No longer blocking for coverage**: the Axon Server arm runs, with
one method supplied by the harness. +
*Claims:* M9 +
*Backend vector:* `axonserver: RUNS (shimmed)`. See `formal/CONNECTOR-COMPATIBILITY.md` for the version table.

=== What this entry used to say, and what was actually true

This entry was originally recorded as *blocking*: "no reachable Axon Server connector artefact can be loaded against
this tree", with the Axon Server arm skipped and the plan's S11 unbuildable. Three of the things it said were wrong, and
correcting them is most of the value of this rewrite.

* *"No reachable artefact" was wrong.* `io.axoniq.framework:axon-server-connector` publishes to Maven Central up to
  `5.2.2`, and 5.2.2 resolves against this reactor with **all 90 of its classes loadable and nothing unresolvable**. The
  artefact was always reachable; the earlier entry was written against a stale local repository whose newest version was
  5.2.0.
* *The package layout in the earlier entry is out of date.* It named
  `org.axonframework.axonserver.connector.event.AxonServerEventStorageEngine`. From 5.1.2 onwards the connector's own
  classes live under `io.axoniq.framework.axonserver.connector.*`, and `javap` on the old name simply reports
  `class not found` -- which is an easy way to conclude wrongly that the artefact is broken.
* *"A missing artefact" was the wrong diagnosis entirely.* This is an **undetected binary-compatibility break between the
  framework and its own released client library**. Commit `d3dc55f338bc9eb3d6e5b32b6c4ef315f952185d`
  ("[#3594] feature(eventsourcing): EventStorageEngine#source and SnapshotStore methods - add ProcessingContext
  parameter") added abstract methods to two SPIs. Nothing in either build catches that, because a caller resolving the
  two-argument form against the *interface* compiles cleanly and only the JVM refuses it.

=== What is wrong

`eventsourcing/src/main/java/org/axonframework/eventsourcing/eventstore/EventStorageEngine.java:137` declares

----
MessageStream<EventMessage> source(SourcingCondition condition, @Nullable ProcessingContext context);
----

as **abstract**, with the one-argument form at `:119` a `default` delegating to it. Every published connector implements
only the one-argument form. Verified against a running server, on the unshimmed engine:

----
java.lang.AbstractMethodError: Receiver class
  io.axoniq.framework.axonserver.connector.event.AxonServerEventStorageEngine
does not define or inherit an implementation of the resolved method
  'abstract org.axonframework.messaging.core.MessageStream source(
     org.axonframework.eventsourcing.eventstore.SourcingCondition,
     org.axonframework.messaging.core.unitofwork.ProcessingContext)'
of interface org.axonframework.eventsourcing.eventstore.EventStorageEngine.
----

The full unimplemented set is **four methods, identical in 5.1.2, 5.2.1 and 5.2.2**, and all four come from that one
commit:

----
AxonServerEventStorageEngine              -> EventStorageEngine.source(SourcingCondition, ProcessingContext)
AggregateBasedAxonServerEventStorageEngine -> EventStorageEngine.source(SourcingCondition, ProcessingContext)
AxonServerSnapshotStore                   -> SnapshotStore.load(QualifiedName, Object, ProcessingContext)
AxonServerSnapshotStore                   -> SnapshotStore.store(QualifiedName, Object, Snapshot, ProcessingContext)
----

=== What unblocked it

Two things, and the second is the more durable of the two.

*A test adapter for one method.* `ContextCarryingAxonServerEngine` subclasses the published engine and implements
`source(SourcingCondition, ProcessingContext)` by dropping the context and calling the connector's own one-argument
implementation. Nothing in the framework and nothing in the connector is modified; this is a harness subclass exactly
like every other backend adapter in the suite. The two snapshot methods are deliberately **not** shimmed, because no
scenario in the suite loads or stores a snapshot, and shimming a method nothing drives would model something nothing
measures.

*What "unimplemented" does and does not mean for the snapshot store.* `AxonServerSnapshotStore` is a real, fully
implemented snapshot store, with working public bodies for `store(QualifiedName, Object, Snapshot)` and
`load(QualifiedName, Object)`. What it lacks is the context-carrying overloads. The snapshot side is the **harder** half
of this break, and the asymmetry matters for the fix: `EventStorageEngine` kept the one-argument `source` as a `default`,
so the connector's method is still a valid override and a three-line shim works, whereas
`eventsourcing/src/main/java/org/axonframework/eventsourcing/snapshot/store/SnapshotStore.java` declares *only* the
context-carrying forms with no `default` -- so the connector's two methods override nothing at all and the class satisfies
neither abstract method. A shim there is still possible, because the bodies exist; adding a `default` is not, because
there is no older form on the interface to delegate from.

*A static compatibility gate.* `ConnectorCompatibilityTest` computes the unimplemented set for a connector artefact
against this reactor in about a second, before any container starts, and fails the build if a method is neither shimmed
nor recorded as undriven. It runs in the default hunt build. Point it at any artefact:

----
./mvnw -q -Phunt -pl simulation -o test -Dtest=ConnectorCompatibilityTest \
    -Dhunt.connectorJar=$HOME/.m2/repository/io/axoniq/framework/axon-server-connector/5.1.2/axon-server-connector-5.1.2.jar \
    -Dsurefire.failIfNoSpecifiedTests=false
----

That turns a ten-minute container run ending in `AbstractMethodError` into a two-second answer naming the methods, and it
is what stops the next such break costing another phase. The version table it produces is
`formal/CONNECTOR-COMPATIBILITY.md`.

=== Residual risk the shim leaves

Stated precisely, because every verdict from the Axon Server arm inherits it.

* *The dropped `ProcessingContext` is not modelled.* The interface's Javadoc at `:127` says the two forms "behave
  identically" and that the context is "passed to decorators (for example snapshot-loading and tracing decorators) that
  need to correlate the sourcing operation with the surrounding unit of work", so a leaf engine that ignores it reads
  events exactly as the connector reads them. What is **not** modelled is any behaviour a future connector might derive
  from the context itself: correlating a sourcing with the unit of work that asked for it, joining a server-side
  transaction, attaching sourcing telemetry. **A finding on this arm that could be explained by the absent context is not
  a finding.** F-19's mechanism was checked against exactly this and does not touch the shimmed method at all.
* *Snapshots are unreachable on this arm.* The connector's snapshot store is fully implemented against the pre-context
  signatures, but neither of the interface's two abstract methods is satisfied and neither is shimmed, so any scenario
  that loaded or stored a snapshot here would fail on a linkage error rather than on anything about the framework. The
  gate records both as undriven so that failure would be self-explaining.
* *The arm is a version combination, not a version.* Framework `5.3.0-SNAPSHOT` crossed with connector `5.2.2` crossed
  with `docker.axoniq.io/axoniq/axonserver:2026.0.0`. Every history the arm writes records all three plus the shimmed
  method in its header's `versions` map, so no verdict from it can be quoted without them.

=== Why it matters

A framework whose own released client library cannot be loaded against its main branch has a broken contract with its
users, and the failure mode is the worst kind: it compiles. Anyone building against `main` with a released connector gets
a clean build and an `AbstractMethodError` at the first sourcing. That is separate from the coverage question this entry
used to be about, and it is the part that is still open.

=== Candidate fix

Structural rather than local. Adding an abstract method to a published SPI is a binary-compatibility break, so either
the new form arrives as a `default` (delegating to the existing one, which is exactly what the harness's shim does) or
the connector is released in lockstep. Note that only `EventStorageEngine` is fixable the first way: `SnapshotStore`
replaced its methods rather than adding to them, so it has no older form to default from and needs either a deprecated
overload pair or the lockstep release. An API-compatibility plugin -- `japicmp` or `revapi` -- on the framework's own
build would have caught this at the commit that introduced it, which is where it is cheapest to catch. The gate added
here catches it downstream, which is second best.

=== Reproduce

----
# The compatibility question, in about a second and with no container.
./mvnw -q -Phunt -pl simulation -o test -Dtest=ConnectorCompatibilityTest \
    -Dsurefire.failIfNoSpecifiedTests=false

# The linkage error itself, against a running server.
./mvnw -Phunt -pl simulation -o test -Dhunt.excludedGroups=fuzz \
    -Dtest='AxonServerBackendTest$TheVersionSkewThisArmWorksAround' \
    -Dsurefire.failIfNoSpecifiedTests=false
----

== F-19 -- Axon Server accepts an append its consistency boundary forbids

*Severity:* High -- a documented consistency guarantee does not hold. +
*Status:* Open, and **not reproducible outside the suite's own path as of the measurement below**. The oracle that found
it still reports it. A committed probe that drives the published connector directly, in three constructions across
fourteen runs, does **not** reproduce it. Mechanism open, and now also *attribution* open. +
*Reproduction state:* *(b) probed.*
`simulation/src/test/java/org/axonframework/hunt/harness/AppendBoundaryEnforcementUnderContentionTest.java` is committed
and reports its numbers every run; it asserts only what one run can decide, and **not** the presence of the effect. +
*Claims:* C1, C5, C6, C7, C8, M9 +
*Backend vector:* `axonserver:FAIL`, `in-memory:PASS`, `hsqldb-tokens:PASS`, `postgres-jpa:N/A (not a boundary store)`,
`postgres-jpa-split-tokens:N/A (not a boundary store)` +
*Observed on:* framework `5.3.0-SNAPSHOT`, connector `io.axoniq.framework:axon-server-connector:5.2.2`, server
`docker.axoniq.io/axoniq/axonserver:2026.0.0`. The arm shims
`EventStorageEngine.source(SourcingCondition, ProcessingContext)`; see the elimination section for why that is not the
cause.

[IMPORTANT]
====
*What this entry used to claim, and why the claim was withdrawn.* It said "Reproduced by test, and reproduced again with
no harness at all", and printed `accepted=83 ... over-accepted appends: 17` as the harness-free measurement. **No test
was ever committed for it**, so that measurement could not be re-run, re-checked or compared against. The probe has now
been written and committed, reconstructed from the shape this entry itself records, and it measures *zero* -- so the
withdrawn sentence was not merely unverifiable, it is not confirmed by the construction it described. What survives is
the suite-side observation, which is real and is reproduced below from the store's own recorded order.
====

=== What is wrong

An append carrying a consistency marker `m` and a boundary `B` must be rejected when the store holds an event matching
`B` at a position at or after `m`. On the suite's path, Axon Server accepts such an append under concurrent conditioned
appends.

The sharpest available statement of the case, which is sharper than this entry previously had it: **the first violation
is an append carrying `ConsistencyMarker.ORIGIN`** -- position `-1`, the condition an append carries when nothing sourced
before it, and the strictest the protocol can express: *no event inside the boundary exists anywhere in the store.* It is
not an append carrying a sourced marker. Read from the run's own history and its own post-run scan:

----
markers across the run's 296 appends: {ORIGIN(-1): 72, INFINITY: 33, 218: 7, 50: 6, 116: 6, ...}

op-17 (writer-0-0)  ORIGIN  boundary=['acct-0']  accepted at position 4
                    -- earlier matching: [(2, 'writer-4-0-w')]
op-18 (writer-3-0)  ORIGIN  boundary=['acct-11'] accepted at position 8   -- earlier matching: []
op-19 (writer-6-0)  ORIGIN  boundary=['acct-7']  accepted at position 6   -- earlier matching: []
op-48 (writer-3-1)  ORIGIN  boundary=['acct-4']  accepted at position 14  -- earlier matching: []
----

Position 2 is below position 4, so the store had already sequenced an `acct-0` event when it sequenced an append that
declared there were none. The window is inside the append operation and it is at the *opening burst*, when the store is
empty and several writers commit at once.

**The rate on the suite's path, which is the number this entry previously did not have:** of the run's 72 ORIGIN appends,
68 were rejected and 4 accepted; three of those four were legitimately the first inside their boundary, and **exactly one
was over-accepted.** One violation per run, not seventeen. The `17` and `24` this entry used to quote came from the
uncommitted probe and are withdrawn -- and they were never the same measurement as the suite's, which is what made them
look like corroboration.

=== Evidence

The suite found it through the reference-model oracle on the shipped contended-append scenario:

----
VECTOR dcb_append_rejected_after_marker_under_contention axonserver:FAIL
  violation: [AppendConformsToDcbModel] ... broken by: an append the model rejects as conflicting was
  recorded as successful | records: [#27, #68]
----

and the history shows the whole picture without any interpretation. `writer-1-0`'s batch is at store positions 6 and 7
and carries `acct-1`; `writer-5-0`'s batch is at positions 10 and 11, was conditioned on marker `0` with boundary
`[acct-1, acct-2]`, and was accepted:

----
   6 writer-1-0-w  tags=[acct-1]  appendIdx=28 marker=0 criteria=[[acct-1],[acct-0]]  completeIdx=57
  10 writer-5-0-w  tags=[acct-2]  appendIdx=27 marker=0 criteria=[[acct-1],[acct-2]]  completeIdx=68
----

The store's own order puts `acct-1` at position 6, before position 10, and `writer-5-0`'s completion after
`writer-1-0`'s.

The suite arm reproduces this on demand. Re-run at the time of writing, on the same version combination:

----
AXONSERVER ARM FAIL framework=5.3.0-SNAPSHOT connector=5.2.2 image=docker.axoniq.io/axoniq/axonserver:2026.0.0
  violation: [AppendConformsToDcbModel] ... broken by: an append the model rejects as conflicting was
  recorded as successful | records: [#22, #57] | seed: 1
  measured: Client verdicts on 296 append(s): 127 acknowledged, 0 event(s) left ambiguous by a failure that
  was not a decision, of which 0 turned out to be stored.
----

Records `#22` and `#57` are the invocation and completion of `writer-0-0`, whose recorded condition is
`marker: -1, criteria: [acct-0]`. That is the ORIGIN case quoted above. Note the record numbers moved between runs
(`#27, #68` then `#22, #57`) while the *shape* did not: under real threads the arm re-samples rather than replays, so a
finding here is pinned by its history file and never by its seed.

=== What the committed probe measures, and what it does not

`AppendBoundaryEnforcementUnderContentionTest` drives the published `AxonServerEventStorageEngine` directly -- not the
harness's context-carrying subclass -- so the shimmed method is absent from the run rather than merely off the append
path. There is no scenario runner, no storage-engine wrapper, no fault injector and no event processor in it. It records
every accepted append's marker, boundary and assigned position, reads the store's own final order back, and looks in the
window between an append's marker and its own position for an event the boundary covers.

Three constructions, fourteen runs, and **every one of them measured zero over-accepted appends**:

[cols="2,1,3"]
|===
| Construction | Runs | Result

| Sourced markers: eight writers, forty rounds each, four hot tags, each round sourcing two tags, taking the lower of the
  two markers, appending a two-event batch under the OR of the two boundaries. *This is the construction the withdrawn
  paragraph above described.*
| 10
| `accepted` 95-104 of 320 per run, 991 in total. `over-accepted appends: 0` every run.

| ORIGIN markers throughout: the same shape, with every append declaring `ConsistencyMarker.ORIGIN` instead of a sourced
  marker.
| 2
| `accepted=3 rejected=317`. `over-accepted appends: 0`.

| The opening burst, isolated: eight writers released at once under one identical boundary, all at ORIGIN, on a store
  emptied between bursts. Exactly one may be accepted.
| 2, of 40 bursts each
| `accepted per burst -> burst count: {1=40}` both times. Exactly one accepted in every burst; 280 rejections.
|===

**The zero is not a vacuous zero, and the probe proves that rather than asserting it.** Three numbers it prints establish
that it looked at real data:

----
PROBE own position minus marker -> count: {0=28, 2=49, 4=22, 6=1}
PROBE accepted appends sequenced above their own marker: 72 (the appends where a violation is observable at all)
PROBE stored events examined inside those windows: 192
PROBE over-accepted appends: 0 of 100 accepted
----

Sixty to seventy-two accepted appends per run land **two, four or six positions above their own marker** -- the exact
window this entry names -- and 160 to 200 stored events sit inside those windows and were each compared against the
accepting append's boundary. The store accepted them because nothing in the gap was inside the boundary, and rejected
216-225 of the 320 attempts because something was. On this construction the guarantee holds.

The probe therefore asserts only what a single run can decide, and the over-acceptance count is *reported, not asserted*:

* every append reached a decision, and at least one was accepted;
* every failure was a consistency rejection, so no append's outcome is unknown -- an unknown outcome would make the
  store's contents unreadable as positions;
* the store holds exactly the accepted appends' events, so a scan index is a global position;
* every accepted append's own batch sits at the position the probe computed for it from the marker the store returned,
  which is what makes the searched window the window it means to search.

=== Why it matters

The consistency marker is the whole of Dynamic Consistency Boundary optimistic concurrency: an application reads a
decision model, decides, and appends on the condition that nothing inside its boundary has changed. If that condition can
pass while the boundary has changed, two concurrent commands can both act on state neither of them still has -- a course
can be overbooked, a balance can go negative, an invariant expressed as a boundary is simply not enforced. It is the
guarantee the whole model rests on, and it is the one users cannot compensate for in application code, because the point
of the boundary is that they do not have to.

The blast radius is exactly the shape production traffic has: concurrent commands touching overlapping boundaries. The
sequential case is correct, so a functional test will not see it.

=== Version-skew and harness elimination, mechanically

The arm links a released connector against a reactor that connector predates and shims one method, so this had to be
ruled out before anything was written up. Each of these was run.

[cols="1,2"]
|===
| Ruled out | How

| The shim
| The shimmed method is `source(SourcingCondition, ProcessingContext)`. The reproduction does not call it: it uses
  `appendEvents` and `commit` directly, both of which the connector implements. The append decision path never touches
  the shim.

| The connector's marker conversion
| `javap -c` on `ConditionConverter.convertAppendCondition` shows
  `GlobalIndexConsistencyMarker.position(...)` passed straight into `setConsistencyMarker(long)` with no arithmetic, and
  the boundary built from `EventCriteria.flatten()` with both tags and names. A faithful pass-through, so the marker the
  server evaluates is the marker the framework computed.

| Marker semantics differing from the reference model
| Measured directly against the server and all four rules agree with the model: a whole-store scan returns global-index
  order; the marker a sourcing reports is the global head, independent of the boundary and of what matched, including
  when the boundary matches nothing; an event exactly **at** the marker counts as a conflict; the marker an accepted
  append reports is its last position plus one.

| A sequential off-by-one
| Six sequential experiments -- marker 0 twice, two criteria against a stored match, a two-tag batch, overlapping but
  different boundary sets, a boundary whose own tag has no event but whose sibling does -- all reject correctly.

| A check-then-commit window between opening the transaction and committing it
| An append transaction was opened on an empty store at marker 0, a conflicting event was fully committed by another
  writer, and the first transaction's `commit()` was then **rejected**. The condition is evaluated at commit, not at open.

| A visibility lag behind the sourcing
| Inserting 150 ms between the last sourcing and the append does not remove the over-acceptances (24 against 17). *Both of
  those counts come from the withdrawn uncommitted probe and are no longer evidence for anything*; the row is kept because
  the experiment was run, and its conclusion -- that a delay between the read and the write changes nothing -- is
  consistent with the committed probe, which finds no over-acceptance at either timing.

| A lagging conflict check under plain concurrency
| Eight writers racing at marker 0 on an empty store, with identical boundaries and again with overlapping-but-different
  boundaries: exactly one accepted in both cases.

| The same burst at ORIGIN rather than at marker 0
| The row above used marker `0`; the violation is always on an append carrying ORIGIN (`-1`), so the burst was repeated at
  ORIGIN. Committed as
  `AppendBoundaryEnforcementUnderContentionTest$EightWritersRacingOnAnEmptyStoreUnderOneBoundary`: forty bursts of eight
  writers under one identical boundary on a store emptied between bursts, `accepted per burst -> burst count: {1=40}`.
  Exactly one accepted in every burst. **The store enforces ORIGIN correctly under a naked burst**, which is what moves
  the open question from the mechanism to the attribution.

| The suite's own oracle
| The same scenario, the same framework, the same workload and the same registered checkers report `PASS` on the in-heap
  boundary store. The divergence is one column of the matrix.
|===

=== One harness defect this finding exposed, fixed here

The first run reported **six** violations, two of them in the opposite direction ("an append the model accepts was
recorded as rejected"). Those two were the harness's, not the store's. `ModelConformanceChecker` applies every append the
store accepted so the replay can continue, but a batch the model *rejects* stores nothing -- while the real store stored
it -- so from the first divergence the model's positions run behind the store's by that batch's size, and a consistency
marker is an absolute position. Measured: model head 246 against a store of 254 events, 242 of 254 positions no longer
lined up, and both "the model accepts" appends do conflict at the store's real positions. The checker now reports the
first mismatch as a violation and every later one as a note, exactly as it already did after an append with an unknown
outcome. **The count of model violations from a run is therefore not a magnitude; only the first one is evidence.**

=== What is still open

*The mechanism*, as before. Every construction that isolates one suspected cause behaves correctly, and settling it needs
Axon Server's own instrumentation rather than a client-side experiment.

*And now the attribution, which is new and is the more urgent of the two.* The suite's oracle reports the violation on
demand; a probe driving the published connector directly, in three constructions over fourteen runs, does not. Both of
those are measurements, and they cannot both be the whole story. What separates the two paths is everything the suite
puts between the workload and the connector -- the framework's `EventStore` and `EventStoreTransaction`, a
`ProcessingContext`, and the harness's own storage-engine wrapper. Three possibilities remain open, and this entry
deliberately names all three rather than picking one:

. *The store's, and the probe's constructions do not hit it.* The probe issues either all-sourced or all-ORIGIN markers;
  the suite's workload mixes them, issuing ORIGIN only on a writer's first append, so its ORIGIN appends race against a
  store that other writers are already growing. The probe's all-ORIGIN run collapses to three acceptances precisely
  because it does *not* have that mix.
. *The framework's.* If the condition the connector is handed differs from the condition the application asked for --
  a marker combined with `lowerBound` across two sourcings in one processing context, a boundary derived differently --
  then the defect is on this side of the wire and the store is enforcing faithfully what it was told.
. *The oracle's.* `ModelConformanceChecker` replays accepted appends in the store's own post-run scan order, which is
  sound, and this suite has already found and fixed one unsoundness in it (below). A second one is not excluded, and the
  probe's zero is the reason to look.

*Deciding between them is a bounded piece of work and it is the next thing to do here:* record, on the suite's path, the
`AppendCondition` as the connector receives it and compare it against the one the workload asked for. That is one
recording point, and it separates possibility 2 from the other two outright.

Until then the severity above stands as *if real*, and this entry may not be quoted as an Axon Server defect. The version
combination in the header is part of the observation, and so is the fact that only one of the two available reproductions
shows it.

=== Candidate fix

Not proposable from outside while the attribution is open. If the store is the cause, what the evidence asks of it is
that the boundary check and the assignment of the global sequence be one atomic step per append, so that an append cannot
be sequenced after an event its check did not see.

=== Reproduce

----
# The suite arm that reports the violation. Under real threads this re-samples rather than replays; the recorded
# history is the exact record, and the record numbers move between runs while the shape does not.
./mvnw -Phunt -pl simulation -o test -Dhunt.excludedGroups=fuzz \
    -Dtest='AxonServerBackendTest$TheShippedCorpusOnAxonServer' -Dsurefire.failIfNoSpecifiedTests=false

# The committed probe: the published connector, driven directly, no harness on the path. Prints its numbers every run
# and asserts only what one run can decide. Three constructions, one @Nested class each.
./mvnw -Phunt -pl simulation -o test -Dhunt.excludedGroups=fuzz \
    -Dtest=AppendBoundaryEnforcementUnderContentionTest -Dsurefire.failIfNoSpecifiedTests=false

# The ORIGIN case the first violation is always an instance of, on its own.
./mvnw -Phunt -pl simulation -o test -Dhunt.excludedGroups=fuzz \
    -Dtest='AppendBoundaryEnforcementUnderContentionTest$EightWritersRacingOnAnEmptyStoreUnderOneBoundary' \
    -Dsurefire.failIfNoSpecifiedTests=false

# The whole matrix, which is where the vector comes from.
./mvnw -Phunt -pl simulation -o test -Dhunt.excludedGroups=fuzz -Dtest=BackendDifferentialTest \
    -Dsurefire.failIfNoSpecifiedTests=false
----

To read the ORIGIN case out of a history the suite arm wrote, which is how the `marker: -1` above was found:

----
python3 - simulation/target/hunt-histories/axonserver-arm/*.jsonl <<'EOF'
import json, sys, collections
recs = [json.loads(l) for l in open(sys.argv[1]) if l.strip()]
order = {e: i for i, e in enumerate(
        [r for r in recs if r.get("op") == "scan"][-1]["value"]["eventIds"])}
tags, cond = {}, {}
for r in recs:
    if r.get("op") == "append" and r.get("type") == "INVOKE":
        for e in r["value"]["events"]:
            tags[e["id"]] = [t["value"] for t in e["tags"]]
        cond[r["id"]] = (r["value"]["marker"], r["value"]["criteria"],
                         [e["id"] for e in r["value"]["events"]])
ok = {r["id"] for r in recs if r.get("op") == "append" and r.get("type") == "OK"}
print("markers:", collections.Counter(m for m, _, _ in cond.values()).most_common(5))
for opid, (m, c, evs) in sorted(cond.items()):
    if m != -1 or opid not in ok:
        continue
    boundary = {t["value"] for cc in c for t in cc["tags"]}
    mine = order.get(evs[0])
    earlier = [(i, e) for e, i in order.items()
               if i < mine and boundary & set(tags.get(e, []))]
    print(opid, "ORIGIN boundary", sorted(boundary), "at position", mine,
          "-- earlier matching:", sorted(earlier))
EOF
----

== F-20 -- The connector reports every commit failure as a consistency rejection

*Severity:* High -- an ambiguous outcome is reported as a definite one, and the cause is discarded. +
*Status:* Open. *Confirmed in the connector's bytecode, and asserted by test.* +
*Reproduction state:* *(a) asserted.*
`AxonServerInfrastructureFailureTest$CuttingTheNetworkWhileLeavingTheServerRunning` asserts that the classification set of
the run's failed appends is *exactly* `{AppendEventsTransactionRejectedException}` after eight landed network cuts. The
day a transport failure surfaces as anything else, that assertion goes red. The set is asserted rather than the count,
because the count varies with where the cuts land and the set does not. +
*Claims:* M5, C29, C35, M9 +
*Backend vector:* `axonserver-chaos:FAIL`, other backends `N/A` -- the mapping is the connector's and no other store has
one. +
*Observed on:* connector `io.axoniq.framework:axon-server-connector:5.2.2` against framework `5.3.0-SNAPSHOT`, server
`docker.axoniq.io/axoniq/axonserver:2026.0.0`.

=== What is wrong

`AxonServerEventStorageEngine.AxonServerAppendTransaction.commit()` installs one error handler for the whole commit and
maps anything it catches to a consistency rejection. From `javap -c` on the shipped 5.2.2 jar:

----
private static java.util.concurrent.CompletionStage lambda$commit$0(java.lang.Throwable);
   0: getstatic     // logger
   3: ldc           // String "Committing append transaction failed."
   6: invokeinterface // Logger.warn(String, Throwable)
  11: new           // class org/axonframework/eventsourcing/eventstore/AppendEventsTransactionRejectedException
  15: aload_0
  16: invokevirtual // Throwable.getMessage()
  19: invokespecial // AppendEventsTransactionRejectedException.<init>(Ljava/lang/String;)V
  22: invokestatic  // CompletableFuture.failedFuture
----

Two separate problems in six instructions. The exception type asserts that *the store decided against the append*, which
is a definite outcome; and the constructor taken is the `String` one, so *the original throwable is not attached as a
cause* and no caller can recover what actually happened. A gRPC `UNAVAILABLE` -- the connection is gone, the append may
or may not have been applied -- arrives at the application as "your append conflicted".

=== Evidence

The partition arm cuts the network eight times, from the store boundary, aimed at the commit. Every cut is evidenced by
the proxy's own reported state. What the run recorded:

----
as-partition fires={store-partition=8}
append record types: {INVOKE=277, FAIL=191, OK=86}
append failure classifications: {AppendEventsTransactionRejectedException=191}
INFO (ambiguous) appends: 0
failed appends whose marker was INFINITY: 18   (all 18 inside a fault window)
----

The eighteen are the proof that the classification is wrong rather than merely coarse. An append carrying an `INFINITY`
marker declares *no* consistency condition, so no correct store can ever reject it as conflicting -- that is the whole
content of the suite's `UnconditionalAppendNeverRejected` invariant, and it is exactly what those eighteen violations
say. All eighteen happened while the network was cut.

The same shape appears on the stream-resume arm: 400 appends, 8 cuts, and `0 event(s) left ambiguous by a failure that
was not a decision`. A run under eight network partitions that produces no ambiguous outcome at all has had its ambiguity
converted into false certainty.

=== Why it matters

This is the acknowledgement-versus-durability question (M5) made actively misleading. An application told "rejected as
conflicting" will re-source its decision model and retry, or report a business conflict to its caller. An application
told "the outcome is unknown" must instead find out whether its events landed. Getting the two the wrong way round means
a command whose events *are* durably stored is reported as having conflicted, and the retry then applies its effects a
second time -- with no conflict to stop it, because the marker has moved on.

It also makes a whole class of defect undiagnosable from the client side, including by this suite's own oracles: a
durability invariant cannot separate "the store said no" from "nobody knows", so it declines to decide. The suite reports
that honestly, which is why the partition arm's durability verdict is undecided rather than green.

=== Version-skew elimination

The harness shims `EventStorageEngine.source(SourcingCondition, ProcessingContext)`. This finding is on the
`appendEvents`/`commit` path, which the connector implements in full and which the shim does not touch. The primary
evidence is the connector's own bytecode from the published artefact, independent of the harness entirely.

=== Candidate fix

Map the server's own consistency rejection to `AppendEventsTransactionRejectedException` and nothing else. Everything
else -- a transport status, a timeout, a closed channel -- is an outcome nobody knows, and should surface as an exception
that says so, distinct from a decision. In both cases pass the original throwable as the cause: the
`AppendEventsTransactionRejectedException(String, Throwable)` form exists, and discarding a cause inside a mapping layer
removes the only evidence a caller could have used.

=== Reproduce

----
./mvnw -Phunt -pl simulation -o test -Dhunt.excludedGroups=fuzz \\
    -Dtest='AxonServerInfrastructureFailureTest$CuttingTheNetworkWhileLeavingTheServerRunning' \\
    -Dsurefire.failIfNoSpecifiedTests=false
----

Then count the classifications in the history the run wrote under
`simulation/target/hunt-histories/axonserver-partition/`, with the reader in `formal/HUNT-NOTES.md` section 3.

== F-21 -- The anti-rewind guard on durable progress is inactive for the first store of every claim

*Severity:* High +
*Status:* Open. Confirmed by measurement; pinned by
`simulation/src/test/java/org/axonframework/hunt/scenario/DurableProgressMonotonicityTest.java`. +
*Claims:* C38 (**REFUTED** -- the claim is documented and is false as stated), M19 +
*Backend verdict vector:* `in-memory:AFFECTED`. Mechanism-attributed rather than store-attributed:
the guard is an in-heap field comparison in `WorkPackage` that runs *before* any store is called, so
no store can express it differently and no store can prevent it. The vector claims one arm and says
why the rest are not informative.

=== What is wrong

The progress-persistence seam added in 5.3.0 lets a processor supply its own
`SegmentProgressStrategy`, which decides which `TrackingToken` is safe and hands it to
`SegmentProgressContext.persistProgress(...)`. The framework undertakes to filter what it is handed:

* `SegmentProgressContext.java:76-78` -- "keeping the stored `TrackingToken` monotonic. A `null`,
  already-stored, or non-advancing token is ignored"
* `WorkPackage.java:507-508` -- "is ignored with a warning rather than persisted, so a misbehaving
  component can never rewind progress on any source."

The guard that enforces it is

[source,java]
----
if (lastStoredToken != null && !TrackingTokenUtils.coversWhenUnwrapped(safe, lastStoredToken)) {
----

`lastStoredToken` is declared without an initialiser and is assigned only after a store succeeds.
The constructor seeds `lastDeliveredToken` and `lastConsumedToken` from the claimed token and leaves
`lastStoredToken` null. A `WorkPackage` is built fresh on every claim of a segment. So on the first
`persistProgress` of every claim cycle the guard is skipped entirely, and any candidate at all is
written -- including one behind the position the store was already holding.

=== Evidence

Code:

* `messaging/src/main/java/org/axonframework/messaging/eventhandling/processing/streaming/pooled/WorkPackage.java:107` -- `private @Nullable TrackingToken lastStoredToken;` (no initialiser)
* `.../pooled/WorkPackage.java:128-147` -- the constructor sets `lastDeliveredToken` (`:136`) and `lastConsumedToken` (`:141`) from `builder.initialToken`; `lastStoredToken` is not set
* `.../pooled/WorkPackage.java:514` -- the guard, conditional on `lastStoredToken != null`
* `.../pooled/WorkPackage.java:530` -- `lastStoredToken = token;`, the only assignment, after a successful store
* `.../pooled/PooledStreamingEventProcessor.java:428-450` -- `spawnWorker` builds a new `WorkPackage` per claim, passing `initialToken` but nothing for `lastStoredToken`

Measured, on the in-heap token store, single node, no faults, no containers:

----
DURABLE PROGRESS before=500 after=0 persists=1 (TrackingToken.FIRST reports position 0)
DURABLE PROGRESS settled=400 after 3 descending candidates from 400 downwards, against a store holding 500
----

The first line: a segment whose durable position was 500 had it rewound to the start of the stream by
the *first* persist of a fresh claim. The processor stayed running, reported no error state, and
raised nothing. The second line: with a strictly descending series of candidates, only the first
(400) was written and every later one was refused -- which measures the window exactly. **One
unguarded write per claim cycle, per segment.**

The candidate offered in the first case is not an arbitrary bad token. It is what the framework's own
`TrackingTokenUtils.lowerBound(...)` -- added by the same feature -- returns for a set of
participants that have not reported a position yet, which is the
"reconciling several participants to a single safe position" use `SegmentProgressStrategy.java:33-35`
documents the seam for. `TrackingTokenUtils.java:49` -- `.orElse(TrackingToken.FIRST)`.

=== Why it matters

The stored token is where a segment resumes. Rewinding it to the start of the stream means the whole
segment is redelivered on the next claim: at-least-once handlers see every event again, a
non-idempotent projection double-counts everything, and the read side spends the replay time
unavailable. The rewind is durable, silent, and leaves the processor healthy -- the only trace is a
`logger.warn` that in this case is not even reached, because the guard did not fire.

Reachability: `progressStrategyFactoryBuilder(...)` is a **public** setter on
`PooledStreamingEventProcessorConfiguration` (`:511`), so any application or downstream module can
install a strategy. The shipped `TokenStoringProgressStrategy` cannot trip the hole, because it only
ever offers `lastConsumedToken`, which starts at the claimed token and only advances -- so no
default-configured application is affected today. What is broken is the guarantee the framework
publishes to the strategies it invites, and that guarantee is what a strategy author will rely on
instead of re-implementing the check.

=== How it was found

By claims-mining the new `progress` package and then reading the guard against the field it compares.
The corpus already carried the promise as C38, anchored and marked `documented`, which is what made
the reading a refutation rather than an observation. No test in the repository exercises
`persistProgress` with a non-advancing candidate on a freshly claimed segment;
`TrackingTokenUtilsTest` tests the comparison helper in isolation, where it behaves correctly.

=== Candidate fix

One line, in the `WorkPackage` constructor:

[source,java]
----
this.lastStoredToken = builder.initialToken;
----

The claimed token is by definition what the store last accepted for that segment, so it is exactly
the right reference. It is also safe for the replay case the guard was carefully written for: the
comparison is on unwrapped upper-bound positions (`TrackingTokenUtils.coversWhenUnwrapped`), so a
concluding replay still reads as an advance.

Worth more than the fix: the guard's reference should not be a field the class happens to fill in
later. Deriving it from the claim would make the invariant hold by construction rather than by
initialisation order.

=== Reproduce

----
./mvnw -Phunt -pl simulation -o test -Dtest=DurableProgressMonotonicityTest \\
    -Dsurefire.failIfNoSpecifiedTests=false
----

Both cases pass while the gap is open and turn red when the constructor seeds the field. Neither
needs Docker, a fault schedule or a seed: the defect is a deterministic property of initialisation
order.

== F-22 -- The broadcast sentinel is a plain string that user data can equal

*Severity:* Medium-High +
*Status:* Open. Confirmed by measurement; pinned by
`simulation/src/test/java/org/axonframework/hunt/scenario/BroadcastSentinelCollisionTest.java`. +
*Claims:* C41, C42, M20 +
*Backend verdict vector:* `in-memory:AFFECTED`. Store-independent by construction: the decision is
two string comparisons on the read side, above every storage engine.

=== What is wrong

The broadcast feature added a well-known sentinel sequence identifier:

* `messaging/src/main/java/org/axonframework/messaging/core/sequencing/SequencingPolicy.java:71` -- `Object BROADCAST = "BROADCAST";`

It is acted on by value, in two places:

* `.../pooled/DefaultWorkPackageEventFilter.java:111` -- `if (sequenceIdentifiers.contains(SequencingPolicy.BROADCAST)) {`
* `.../processing/ProcessorEventHandlingComponents.java:194` -- `if (SequencingPolicy.BROADCAST.equals(sequenceIdentifier)) {`

Every value-extracting policy the framework ships returns a `String` taken from the event or its
metadata: `MetadataSequencingPolicy` returns a metadata value, `PropertySequencingPolicy` and
`ExtractionSequencingPolicy` return an extracted field, and `SequentialPerAggregatePolicy` returns a
bare entity identifier. Any of them can return the exact string `BROADCAST` without anybody asking
for a broadcast, and the framework then delivers that event once per segment instead of once.

The collision surface was widened on purpose, for cosmetic reasons. Commit `9ab376f451`
("Use the short constant name BROADCAST with a matching \"BROADCAST\" value, so the name and value
agree") changed the value from `"BROADCAST_SEQUENCE_IDENTIFIER"` -- which no payload plausibly
carries -- to `"BROADCAST"`, which a tenant, topic, channel or entity identifier plausibly does.

Note the contrast with the framework's own convention for the analogous sentinel one file away:
`SequentialPolicy.java:46` keeps the long screaming value `"FULL_SEQUENTIAL_POLICY"` and marks it
`@Internal`. `BROADCAST` is public API with neither guard.

=== Evidence

Measured, four segments, no store, no timing, fully deterministic:

----
BROADCAST COLLISION segments=4 handled[orders]=1 handled[BROADCAST]=4
BROADCAST COLLISION deliberate=4 accidental=4
----

The first line: driving the same component over all four segments, an ordinary sequence key was
handled once in total and a key whose *data value* is `BROADCAST` was handled four times -- once per
segment. The second line: a component deliberately returning `SequencingPolicy.BROADCAST` and a
component merely carrying that value as data are treated identically, so nothing downstream can
distinguish intent from data.

=== Why it matters

The amplification factor is the live segment count, which defaults to 16
(`docs/reference-guide/modules/events/pages/event-processors/streaming.adoc:563`). A non-idempotent
projection therefore applies one such event sixteen times. Because the factor is the *live* segment
count, it also changes silently on every split and merge, so the same event is amplified differently
at different times in one deployment's life.

It is silent in both directions: nothing warns the author of a policy that one of its values is
reserved, and nothing tells an operator that an event was broadcast rather than routed.

=== Also wrong, and cheaper to fix: the class contract now contradicts itself

`ProcessorEventHandlingComponents.java:116` still states, in the Javadoc of the public `handle`
method, "ensuring each event is handled exactly once per component across all segments". With a
broadcast identifier that component is invoked once per segment. The feature updated the private
`canHandleInSegment` Javadoc (`:176-178`) and left the public contract asserting the opposite, so a
reader of the class-level contract will correctly conclude that broadcast is a bug.

=== How it was found

By mining the feature's claims and asking what the sentinel is made of. The commit history is what
turned it from a theoretical collision into a live one: the value used to be collision-implausible
and was shortened deliberately.

=== Candidate fix

Make the sentinel not a value user data can equal. Cheapest form that keeps the current shape:

[source,java]
----
Object BROADCAST = new Object() {
    @Override public String toString() { return "BROADCAST"; }
};
----

compared by identity. That keeps `hashCode`/`equals` stability irrelevant, because the sentinel is
never hashed into a segment -- both call sites short-circuit before `SegmentMatcher` sees it. If the
`String` form must be kept for cross-JVM stability of a *serialised* policy result, namespace it
(`"org.axonframework.BROADCAST"`) and say in the Javadoc that a policy must not return it as data.
Better still, make it a type -- a `BroadcastSequenceIdentifier` singleton -- so a policy returning
data can never name it by accident.

Separately, correct `ProcessorEventHandlingComponents.java:116` to state the per-segment case.

=== Reproduce

----
./mvnw -Phunt -pl simulation -o test -Dtest=BroadcastSentinelCollisionTest \\
    -Dsurefire.failIfNoSpecifiedTests=false
----

== F-23 -- The Spring Boot token-store default makes the new optional-token-store path unreachable

*Severity:* Medium +
*Status:* Open. Confirmed by reading; no test exists yet. +
*Claims:* C43, M21 +
*Backend verdict vector:* `postgres-jpa:AFFECTED (Spring Boot path only)`, `in-memory:N/A`,
`axonserver:N/A`. Same shape and same configuration path as F-1.

=== What is wrong

Commit `bc68fc2da7` ("Make pooled processor source and token-store optional when unset") made the
Spring customization treat a null-or-empty `source` and `tokenStore` setting as unset, resolving the
component by type instead and requiring it only when a *name* was given:

* `extensions/spring/spring/src/main/java/org/axonframework/extension/spring/config/SpringCustomizations.java:171` -- `String tokenStoreName = StringUtils.nonEmptyOrNull(settings.tokenStore()) ? settings.tokenStore() : null;`
* `.../SpringCustomizations.java:173-175` -- `require(tokenStore != null, "Could not find a mandatory TokenStore with name '" + settings.tokenStore() + "' ...")`

The settings object Spring Boot actually supplies defaults that field to a non-empty literal:

* `extensions/spring/spring-boot-autoconfigure/src/main/java/org/axonframework/extension/springboot/EventProcessorProperties.java:156` -- `private String tokenStore = "tokenStore";`

So `nonEmptyOrNull` is always true, `tokenStoreName` is never null, and the "unset" branch is never
taken through Spring Boot properties. The feature's own headline behaviour is dead on the path most
applications use.

The asymmetry is the tell: `source` defaults to `null` on the same properties class
(`EventProcessorProperties.java:114`), so the optional path *is* reachable for the source and never
for the token store, from one commit that presents both as one uniform rule.

=== Why it matters

An application with no bean named exactly `tokenStore` now fails at startup with
"Could not find a mandatory TokenStore with name 'tokenStore'", where the core configurer would have
substituted an `InMemoryTokenStore`
(`.../pooled/PooledStreamingEventProcessorsConfigurer.java:93` -- `registerIfNotPresent(TokenStore.class, cfg -> new InMemoryTokenStore())`).
The JPA and JDBC autoconfigurations happen to name theirs `tokenStore`, which is why this is not
visible in the common cases and why it will be found by whoever first wires a custom one.

The corroboration is in the feature's own tests: the new unit tests drive
`customizePooled(configuration, null, null)` and `("", "")` through a hand-rolled test settings
record rather than through `ProcessorSettings`, and the new integration test contexts each declare a
bean named exactly `tokenStore` to make the context boot at all. The gap between the settings object
under test and the settings object shipped is exactly where the defect lives.

=== Candidate fix

Default `EventProcessorProperties.ProcessorSettings.tokenStore` to `null`, matching `source`. Then
both halves of "optional when unset" mean what the commit says. `setTokenStore` currently forbids
null (`EventProcessorProperties.java:334`), which should become "forbids null only when explicitly
set" or simply allow it.

The durable guard is the same one F-1 asks for: a test that builds a processor through both
configuration paths and asserts every effective setting is pairwise equal or on an explicit
allow-list of deliberate differences. Two findings in this file are now instances of that one class.

=== Reproduce

No automated reproduction yet. By inspection:

----
grep -n 'private String tokenStore' \\
  extensions/spring/spring-boot-autoconfigure/src/main/java/org/axonframework/extension/springboot/EventProcessorProperties.java
sed -n '171,180p' \\
  extensions/spring/spring/src/main/java/org/axonframework/extension/spring/config/SpringCustomizations.java
----
