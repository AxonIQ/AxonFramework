/**
 * Event-sources the process from the same audit events as the command-recorded variant, but appends them directly from
 * event handlers. Sourcing state first supplies an append condition; appending without sourcing would be unconditional.
 */
package org.axonframework.examples.sagarecipes.saga.eventsourcedappend;
